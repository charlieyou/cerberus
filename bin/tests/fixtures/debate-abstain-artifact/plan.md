# Plan: ship a small CLI tool

## Steps

1. Read configuration from a YAML file at startup.
2. Execute the requested subcommand.
3. Emit structured logs.

## Notes

This plan is intentionally generic — the fixture's test condition is
the abstain behavior of one reviewer, not the substantive critique of
this content.
