<!--
  Generated by /cerberus:create-tasks --agent-team from docs/2026-04-30-port-shell-to-go-plan.md.
  Conforms to templates/team-tasks-template.md. Consumed by /cerberus:run-team.
-->

---
plan: docs/2026-04-30-port-shell-to-go-plan.md
spec: N/A
generated: 2026-05-01T00:00:00Z
---

# Team Tasks: Port Cerberus Shell Scripts to Go

**Generated**: 2026-05-01T00:00:00Z
**Plan**: [docs/2026-04-30-port-shell-to-go-plan.md](2026-04-30-port-shell-to-go-plan.md)
**Spec**: N/A — derived from user description; legacy contract is `README.md`, `hooks/hooks.json`, `templates/codex-hooks.json`, `docs/CODEX.md`, `docs/AMP.md`.

## Overview

Replace ~33,400 LOC of bash (8 production binaries, 5 shared libs, 32 test files, the Amp toolbox dispatcher) with a single Go module producing per-binary executables that preserve the existing CLI surface, hook contracts, on-disk schemas, and host-neutral env contract byte-for-byte. Rollout is four sequential hard-cutover phases after a non-cutover foundation phase; each phase is a tagged release with `git revert` rollback.

## Task Summary

| Phase | Tasks | Dependencies |
|-------|-------|--------------|
| Setup | T001, T002, T003 | T001 → none; T002 → T001; T003 → T001, T002 |
| Foundation | T004, T005, T006, T007, T008, T009, T010 | All depend on T001 (and T002 for T009) |
| Phase 1 — Core review-gate | T011, T012, T013, T014, T015, T016 | T011 → Foundation; T012-T015 → T011; T016 → T012-T015 |
| Phase 2 — Hooks + generator | T017, T018, T019, T020, T021, T022, T023 | T017 → T011; impls → T017, T018; T023 → T019-T022 |
| Phase 3 — Debate + Amp | T024, T025, T026, T027, T028, T029, T030, T031, T032 | T024 → T017, T018; T029 → T024; T031 → T028, T030 |
| Phase 4 — Cleanup | T033, T034, T035 | T033, T034 → T031; T035 → T033 |

---

## T001 — Setup: Initialize Go module + Makefile + .gitignore + workflow skeleton
```meta
phase: Setup
files: [go.mod, go.sum, Makefile, .gitignore, .github/workflows/test.yml]
depends: []
acceptance: [AC12]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L127-L208
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L127-L208](2026-04-30-port-shell-to-go-plan.md#L127-L208) — Section: Prerequisites
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L755-L835](2026-04-30-port-shell-to-go-plan.md#L755-L835) — Section: Distribution mechanic (Makefile)
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L78-L96](2026-04-30-port-shell-to-go-plan.md#L78-L96) — Section: Decision Log (module path, distribution, Go 1.25)
- Spec: N/A

### Files

- `go.mod` (New) — `module github.com/charlieyou/cerberus`, `go 1.25`
- `go.sum` (New) — pinned deps after `go mod tidy`
- `Makefile` (New) — targets `build`, `test`, `bench`, `golden-capture`, `golden-diff`, `refresh-goldens`, `install-binaries`; per-cmd Go-build pattern + compat-wrapper pattern + atomic dist swap (uses `$(shell find internal -type f -name '*.go')` for cross-version Make portability). `refresh-goldens` re-runs bash production through `bin/tests/golden/normalize.sh` to update the captured baseline when an intentional behavior change lands; the diff that lands in the PR is the only place new bash output is approved as canonical (per plan §Golden fixture normalization L1018).
- `.gitignore` (Modify) — add single pattern `/dist*/` (covers `dist/`, `dist.new/`, `dist.old/`)
- `.github/workflows/test.yml` (New, skeleton; full wiring in T010) — minimum viable trigger so subsequent CI tasks have a workflow to extend

### Depends

- None

### Goal

Land the foundational build/distribution scaffolding so every later task has a working `go build` and Makefile target. No user-visible behavior change yet.

### Implementation Notes

- Module path locked at `github.com/charlieyou/cerberus` (Decision Log row 1; `README.md` line 56 marketplace path).
- Pin three deps via `go mod tidy`: `github.com/gofrs/flock` (production), `github.com/stretchr/testify` (test-only), `github.com/google/go-cmp` (test-only). All other behavior must come from stdlib; new deps require a Decision Log entry.
- Makefile must include both pattern rules (Go-build with prereqs, prereq-less compat-wrapper) per the Distribution mechanic block. `GO_CMDS := review-gate generate cerberus-task-completed-hook cerberus-teammate-idle-hook claude-session-init codex-session-init codex-stop-hook cerberus-amp-toolbox`.
- Atomic swap recipe: build into `dist.new/` (sibling of `dist/` at repo root), `mv dist dist.old && mv dist.new dist && rm -rf dist.old`, with EXIT trap restoring `dist.old → dist` on failure.
- `.PHONY` declarations for `install-binaries`, `build`, `test`, `bench`, `golden-capture`, `golden-diff`, `install-compat-wrappers`.
- Workflow file in this task is intentionally minimal — T010 wires the matrix, build steps, golden diff, and bench.

### Verification

- `go mod tidy` succeeds; `go.sum` committed.
- `make install-binaries` succeeds even with no `cmd/` directories yet (compat-wrapper rule fires; needs T002's `.bash-prod` files in place — but the Makefile rule itself must be syntactically valid).
- `git status` clean after `make install-binaries` (only `dist/` is written; `dist*/` pattern excludes it).
- Negative test: `make install-binaries` with a deliberately broken pattern rule must fail loudly without leaving partial `dist/` (atomic swap holds).

---

## T002 — Setup: Commit 8 permanent shims + rename production scripts to .bash-prod + bridge wrapper
```meta
phase: Setup
files: [bin/review-gate, bin/generate, bin/cerberus-task-completed-hook, bin/cerberus-teammate-idle-hook, bin/claude-session-init, bin/codex-session-init, bin/codex-stop-hook, .amp/toolbox/cerberus.sh, bin/review-gate-debate-bridge.sh]
depends: [T001]
acceptance: [AC1, AC12]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L130-L188
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L130-L167](2026-04-30-port-shell-to-go-plan.md#L130-L167) — Section: Prerequisites — Templates A & B
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L168-L188](2026-04-30-port-shell-to-go-plan.md#L168-L188) — Section: Prerequisites — review-gate-debate-bridge.sh wrapper
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L837-L874](2026-04-30-port-shell-to-go-plan.md#L837-L874) — Section: Distribution mechanic — shim contents
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L277-L298](2026-04-30-port-shell-to-go-plan.md#L277-L298) — Section: Architecture (bin/ layout)
- Spec: N/A

### Files

- `bin/review-gate` (Replace existing 5767-LOC script with Template A shim) — also `git mv` original to `bin/review-gate.bash-prod`
- `bin/generate` (Replace + rename to `.bash-prod`) — Template A
- `bin/cerberus-task-completed-hook` (Replace + rename) — Template A
- `bin/cerberus-teammate-idle-hook` (Replace + rename) — Template A
- `bin/claude-session-init` (Replace + rename) — Template A
- `bin/codex-session-init` (Replace + rename) — Template A
- `bin/codex-stop-hook` (Replace + rename) — Template A
- `.amp/toolbox/cerberus.sh` (Replace existing 564-LOC dispatcher with Template B shim; rename original to `.amp/toolbox/cerberus.sh.bash-prod`) — Template B (NOT Template A)
- `bin/review-gate-debate-bridge.sh` (New, ~12 LOC) — sources `review-gate-lib.sh`, `telemetry-lib.sh`, `review-gate-models.sh`, `review-gate-debate.sh`, then calls `run_debate_coordinator "$@"`

### Depends

- T001 (Makefile + `.gitignore` must exist so the compat-wrapper rule produces a working `dist/<name>` after this task lands)

### Goal

Establish the load-bearing **canonical-shim invariant**: `bin/<name>` and `.amp/toolbox/cerberus.sh` are stable committed shims; legacy bash logic moves to `<name>.bash-prod`; the Phase 1+2 Go bridge has an exec'able wrapper to target. After this task ships, `make install-binaries` must produce a working `dist/<name>` for every binary in `GO_CMDS` (compat-wrappers delegating to the renamed `.bash-prod` files). No user-visible behavior change.

### Implementation Notes

- **Two distinct shim templates** — Template A is generic and used for the seven `bin/<name>` paths; Template B is Amp-specific and used ONLY at `.amp/toolbox/cerberus.sh`. Template B walks up two directory levels (`../..`) and hard-codes the artifact name `dist/cerberus-amp-toolbox` because (1) `.amp/toolbox/../dist/...` resolves to `.amp/dist/`, not the repo-root `dist/`, and (2) `basename "$0"` would yield `cerberus.sh`, which does not exist under `dist/`.
- All shims contain the `cerberus-shim v1` sentinel comment in line 2; this is the marker `bin/update-plugin` greps for in T003 to detect direct-binary substitution.
- The `.amp/toolbox/cerberus.sh.bash-prod` form **must be kept on disk through Phase 2** so the Phase 0–2 Amp compat-wrapper has bash logic to delegate to. It is deleted in T031 (Phase 3 release gate), not now.
- Bridge wrapper exists because `bin/review-gate-debate.sh` only defines functions and has no top-level invocation — executing it directly returns immediately without running the coordinator. The wrapper is the smallest stable exec'able entry point.
- The five shared libs (`review-gate-lib.sh`, `review-gate-models.sh`, `review-gate-hook.sh`, `review-gate-debate.sh`, `telemetry-lib.sh`) **keep their `.sh` names unchanged** through Phase 4 because they are sourced, not exec'd. There is no `review-gate-debate.sh.bash-prod` — only production binaries are renamed.

### Verification

- `git diff --stat` shows 8 shim files added/replaced (each ~10 lines) and 7 `.bash-prod` files renamed in.
- All 8 shims start with `#!/usr/bin/env bash` and have line 2 matching the sentinel `^# cerberus-shim v1`.
- `make install-binaries` after this task lands produces `dist/<name>` for every binary in `GO_CMDS`; each compat-wrapper exec invokes the corresponding `.bash-prod` and forwards args (sanity test: `dist/review-gate --help` produces the same output as `bash bin/review-gate.bash-prod --help`).
- Negative test: deleting any `.bash-prod` and running the corresponding `dist/<name>` must fail with bash's exec error (compat-wrapper does not silently swallow).
- All seven `bin/<name>` shims pass the sentinel grep: `for c in review-gate generate cerberus-task-completed-hook cerberus-teammate-idle-hook claude-session-init codex-session-init codex-stop-hook; do grep -q '^# cerberus-shim v1' "bin/$c" || exit 1; done && grep -q '^# cerberus-shim v1' .amp/toolbox/cerberus.sh`.

---

## T003 — Setup: Edit bin/update-plugin (pre-pull guard, post-pull rebuild handling, sentinel check)
```meta
phase: Setup
files: [bin/update-plugin]
depends: [T001, T002]
acceptance: [AC12]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L195-L208
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L195-L208](2026-04-30-port-shell-to-go-plan.md#L195-L208) — Section: Prerequisites — update-plugin rebuild-failure UX
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L988-L989](2026-04-30-port-shell-to-go-plan.md#L988-L989) — Section: Risks — Shim/dist drift, Update-plugin rebuild-failure UX
- Spec: N/A

### Files

- `bin/update-plugin` (Modify) — add pre-pull Go-toolchain guard, sentinel check on canonical shims, post-pull rebuild call with `rebuild_rc` capture and explicit failure message

### Depends

- T001 (Makefile target `install-binaries` exists)
- T002 (canonical shims with `cerberus-shim v1` sentinel exist)

### Goal

Ensure the bash-only `bin/update-plugin` upgrades the plugin safely: aborts before pulling if Go 1.25 is missing; refuses to proceed if any canonical shim has been replaced; runs `make install-binaries` after pull; reports rebuild failure with an actionable recovery message and propagates the rebuild exit code.

### Implementation Notes

- **Pre-pull guard**: `command -v go >/dev/null && go version | grep -q 'go1.25'`. On failure, abort BEFORE `git pull` with the exact stderr message specified in the plan, naming Go 1.25 and explaining that pulling would leave the plugin broken.
- **Sentinel check**: before invoking the build, `grep -q '^# cerberus-shim v1' bin/<name>` for each canonical shim path AND `.amp/toolbox/cerberus.sh`. The `(Amp)` parenthetical on Template B does not affect the prefix match. On mismatch, abort with a loud error pointing the user at `git restore <path>` and `make install-binaries`.
- **Post-pull rebuild**: `make install-binaries; rebuild_rc=$?`. On non-zero `$rebuild_rc`, print the exact recovery message specified in the plan and exit with the same `rebuild_rc` so callers/CI surface the failure.
- The pull is now safe because `make install-binaries` only writes inside the gitignored `dist*/` siblings; `git pull` will never abort with "local changes would be overwritten by merge".
- `bin/update-plugin` itself remains bash (Decision Log row "`bin/update-plugin` left as bash, out of scope"); this is a minor edit only.

### Verification

- Manual: rename `go` out of `PATH`, run `bin/update-plugin`; pre-pull guard fires with exact stderr text including "Go 1.25 toolchain not found in PATH; refusing to pull".
- Manual: replace `bin/review-gate` with a non-shim file, run `bin/update-plugin`; sentinel check fires with `git restore` recovery message.
- Manual: break `cmd/review-gate/main.go` (after Phase 1 lands) and run `bin/update-plugin`; rebuild failure prints the recovery message and exits with the captured `rebuild_rc`.
- New `bin/tests/test-update-plugin.sh` test cases cover all three failure modes.

---

## T004 — Foundation: internal/cerberusenv with full env precedence + alias-divergence warning
```meta
phase: Foundation
files: [internal/cerberusenv/env.go, internal/cerberusenv/resolve.go, internal/cerberusenv/host.go, internal/cerberusenv/env_test.go, internal/cerberusenv/resolve_test.go]
depends: [T001]
acceptance: [AC3]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L305-L356
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L305-L356](2026-04-30-port-shell-to-go-plan.md#L305-L356) — Section: Technical Design — internal/cerberusenv
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L59](2026-04-30-port-shell-to-go-plan.md#L59) — Section: Testing Constraints — env precedence checklist item
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L1021](2026-04-30-port-shell-to-go-plan.md#L1021) — Section: Testing & Validation — cerberusenv unit tests
- Spec: N/A

### Files

- `internal/cerberusenv/env.go` (New) — `Host` enum, `Env` struct with `Root`/`StateRoot`/`ProjectKey`/`SessionID`/`RunKey`/`TranscriptPath`/`ProjectDir`
- `internal/cerberusenv/resolve.go` (New) — `Resolve()`, `ResolveRunKey()`, `ResolveRoot()`, `ResolveSessionID()`, `ResolveTranscriptPath()`, `ProjectHash()`, `ExportNeutralEnv()`; `sync.Once` gate for alias-divergence warning
- `internal/cerberusenv/host.go` (New) — host detection (claude/codex/amp/generic)
- `internal/cerberusenv/env_test.go` (New) — table-driven tests for each variable
- `internal/cerberusenv/resolve_test.go` (New) — alias precedence + divergence warning tests

### Depends

- T001

### Goal

Port `__cerberus_resolve_*` from `bin/review-gate-lib.sh` into a Go package that preserves alias precedence (canonical `CERBERUS_*` wins over legacy when both non-empty) and emits a single stderr warning per process via `sync.Once` when a canonical and an alias disagree. This is the host-neutral env contract `README.md` lines 478–491 guarantees indefinitely.

### Implementation Notes

- **TDD steps**: write failing tests first asserting (a) canonical-only resolution; (b) alias-only resolution; (c) both-equal (no warning); (d) both-disagree (exactly one warning per process); (e) both-empty. Then implement.
- Aliases per the plan and `README.md`:
  - `CERBERUS_RUN_KEY` ↔ `REVIEW_GATE_SESSION_KEY`
  - `CERBERUS_ROOT` ↔ `CLAUDE_PLUGIN_ROOT`
  - `CERBERUS_SESSION_ID` ↔ `CLAUDE_SESSION_ID`
  - `CERBERUS_TRANSCRIPT_PATH` ↔ `CLAUDE_TRANSCRIPT_PATH`, `REVIEW_GATE_TRANSCRIPT_PATH`
- `ProjectHash` mirrors `get_project_hash()`: SHA256 of `EvalSymlinks(cwd or root)` truncated to 12 hex chars. Use `crypto/sha256` + `filepath.EvalSymlinks` — no shellout.
- `sync.Once` semantics replace the `$$`-keyed marker file used by bash's `__cerberus_resolve_run_key` (the marker file becomes irrelevant in Go but observable behavior — one warning per process — is preserved).
- `ExportNeutralEnv(base, env)` returns `os.Environ()`-shaped slice with both canonical `CERBERUS_*` and legacy aliases set, for handing to bash bridges or reviewer subprocesses.
- This is the **integration-path-test feature** for the foundation phase (see T009 for the system-wide integration via golden capture).

### Verification

- `go test ./internal/cerberusenv/... -race -cover` green.
- Table-driven test asserts exactly ONE divergence warning per process across multiple `Resolve()` calls.
- Negative case: empty values are treated as unset (not the empty string).
- Existing `bin/tests/test-host-neutral-state.sh` exercises external surface and continues to pass.

---

## T005 — Foundation: internal/jsonutil — MarshalStable + Canonicalize + Repair + PathsScalars
```meta
phase: Foundation
files: [internal/jsonutil/marshal.go, internal/jsonutil/canonical.go, internal/jsonutil/repair.go, internal/jsonutil/paths.go, internal/jsonutil/marshal_test.go, internal/jsonutil/canonical_test.go, internal/jsonutil/repair_test.go]
depends: [T001]
acceptance: [AC2]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L404-L448
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L404-L448](2026-04-30-port-shell-to-go-plan.md#L404-L448) — Section: Technical Design — internal/jsonutil
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L1003-L1019](2026-04-30-port-shell-to-go-plan.md#L1003-L1019) — Section: Testing — Golden fixture normalization
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L973-L976](2026-04-30-port-shell-to-go-plan.md#L973-L976) — Section: Risks — JSON key ordering, jq filter parity, python3 JSON-repair
- Spec: N/A

### Files

- `internal/jsonutil/marshal.go` (New) — `OrderedObject`, `MarshalStable`, `MarshalCompactStable`, `UnmarshalObject`, `PatchObject`
- `internal/jsonutil/canonical.go` (New) — `CanonicalOpts`, `Canonicalize` (timestamp/PID/ID/path/duration field masks)
- `internal/jsonutil/repair.go` (New) — `Repair` (lenient parse: strip BOM, recover trailing commas at the specific bash callsites)
- `internal/jsonutil/paths.go` (New) — `PathsScalars` (jq `paths(scalars) as $p | $p | join(".")` equivalent)
- `internal/jsonutil/*_test.go` (New) — byte-identical tests for whitespace, key order, trailing newline; canonicalization mask tests

### Depends

- T001

### Goal

Provide the JSON utilities that replace `jq`, `python3 -c '…'`, and stdlib `json.Marshal`'s sort-by-key behavior so every persisted struct can produce byte-stable output that golden tests can diff. Centralize the canonicalization layer used identically by both bash-captured goldens and Go-produced output.

### Implementation Notes

- `MarshalStable` MUST preserve key order matching the captured golden output (which derives from `jq`'s emit order). Use explicit struct tags + a custom marshal that walks declared fields in source order; do NOT rely on `map[string]any` for persisted state.
- `Canonicalize` is the **single normalization function** applied identically to bash-captured goldens and Go-produced output before byte comparison. Field-mask list (`internal/jsonutil/canonical.go`) is reviewed at every Decision Log update.
  - Timestamps (`*.created_at`, `*.updated_at`, `*.resolved_at`, `*.iteration_*.{started,ended}_at`) → `<TIMESTAMP>`
  - PIDs (`*.pid`, `*.coordinator_pid`, `*.reviewers.*.pid`) → `<PID>`
  - Random/UUID-shaped IDs (`*.run_id`, `*.session_id`, `*.iteration_id` when random) → `<ID>`
  - Ephemeral paths (`${TMPDIR}/cerberus-*`, `dist.new/*`) → `<TMP>`
  - Duration fields (`elapsed_ms`, `wall_clock_seconds`, `latency_ms`) → `<MS>`
- `Repair` mirrors `python3 -c '...'` lenient-parse semantics from `bin/review-gate-lib.sh` and `bin/telemetry-lib.sh`. Identify exact callsites (T009's golden capture uses these inputs) and add per-callsite test fixtures.
- **Anti-pattern**: adding a field mask to silence a real diff. New masks require a separate justification in the PR description AND a new entry in the Decision Log.

### Verification

- `go test ./internal/jsonutil/... -race -cover` green.
- Whitespace-only / key-order / trailing-newline differences yield byte-identical output through `Canonicalize`.
- Negative test: feeding malformed JSON without the recoverable patterns must error rather than silently produce empty output.
- `MarshalStable` against the 26 JSON fixtures in `bin/tests/fixtures/` produces output matching the captured `jq`-emitted equivalents (byte-identical after `Canonicalize`).

---

## T006 — Foundation: internal/procgroup — Detach (Setsid) + Supervise (Setpgid) with integration tests
```meta
phase: Foundation
files: [internal/procgroup/procgroup.go, internal/procgroup/detach.go, internal/procgroup/supervise.go, internal/procgroup/procgroup_test.go, internal/procgroup/integration_test.go]
depends: [T001]
acceptance: [AC6]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L487-L522
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L487-L522](2026-04-30-port-shell-to-go-plan.md#L487-L522) — Section: Technical Design — internal/procgroup
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L68](2026-04-30-port-shell-to-go-plan.md#L68) — Section: Testing Constraints — procgroup detach + supervise checklist
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L972](2026-04-30-port-shell-to-go-plan.md#L972) — Section: Risks — reviewer subprocess detach on macOS
- Spec: N/A

### Files

- `internal/procgroup/procgroup.go` (New) — types (`Process`), `StartDetached`, `StartSupervised`, `StartWithTimeout`, `SignalGroup`, `KillGroup`, `Wait`
- `internal/procgroup/detach.go` (New, Unix build tag) — `Detach(*exec.Cmd)` sets `SysProcAttr.Setsid = true` on both Linux + Darwin
- `internal/procgroup/supervise.go` (New, Unix build tag) — `Supervise(*exec.Cmd)` sets `SysProcAttr.Setpgid = true` (Pgid = 0)
- `internal/procgroup/procgroup_test.go` (New) — unit tests for SysProcAttr setup
- `internal/procgroup/integration_test.go` (New) — Detach test (subprocess survives parent exit, records own session id) + Supervise test (signal forwarder delivers to child pgid, exit code propagated)

### Depends

- T001

### Goal

Provide two distinct subprocess patterns: **Detach** (reviewer subprocesses must survive parent exit; replaces bash `setsid`) and **Supervise** (bridged bash debate coordinator runs under Go's lifetime with signal forwarding). Both patterns must be symmetric across Linux + Darwin.

### Implementation Notes

- **Detach** (used for reviewer subprocesses launched by Go: `claude`, `codex`, `gemini`): `SysProcAttr{Setsid: true}` on **both Linux and Darwin** (Go's `syscall` calls `setsid(2)` directly on macOS — no `setsid` binary needed). Caller invokes `Cmd.Process.Release()` after `Start`. Go does NOT signal-forward to detached children.
- **Supervise** (used for the bridged bash debate coordinator in Phases 1+2): `SysProcAttr{Setpgid: true}` (Pgid = 0 → child becomes its own pgid leader). Caller pairs this with a `signal.Notify` forwarder that calls `syscall.Kill(-cmd.Process.Pid, sig)` for SIGINT/SIGTERM/SIGHUP/SIGQUIT/SIGUSR1.
- Integration tests use a small helper subprocess (compiled from `internal/procgroup/testdata/`) that records its own pid/sid/pgid to a file the test reads back.
- Race-detector clean: `go test -race`.
- This task includes the **Detach + Supervise checklist item** from the load-bearing behavioral checklist (Phase 0 covers both).

### Verification

- `go test ./internal/procgroup/... -race -cover` green on both Linux and macOS in CI.
- Detach test: parent exits, helper subprocess remains alive in its own session for 2s, then self-terminates; `wait` on a separate watcher confirms it ran past parent exit.
- Supervise test: parent receives SIGTERM, forwarder delivers to child pgid (`syscall.Kill(-pid, SIGTERM)`), child captures the signal, child exit code is propagated by parent (`cmd.ProcessState.ExitCode()`).
- Negative test: Detach without `Process.Release()` leaks the child as a zombie (assert via `wait` returning a non-zero status); guides correct usage.

---

## T007 — Foundation: internal/state skeleton + flock cross-runtime interop test
```meta
phase: Foundation
files: [internal/state/store.go, internal/state/gate.go, internal/state/lock.go, internal/state/atomic.go, internal/state/store_test.go, internal/state/lock_interop_test.go]
depends: [T001, T005]
acceptance: [AC2, AC8]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L358-L402
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L358-L402](2026-04-30-port-shell-to-go-plan.md#L358-L402) — Section: Technical Design — internal/state
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L60](2026-04-30-port-shell-to-go-plan.md#L60) — Section: Testing Constraints — state machine transitions
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L63](2026-04-30-port-shell-to-go-plan.md#L63) — Section: Testing Constraints — lock interop test
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L970](2026-04-30-port-shell-to-go-plan.md#L970) — Section: Risks — cross-runtime flock interop
- Spec: N/A

### Files

- `internal/state/store.go` (New) — `Store`, `NewStore`, `ReviewDir`, `ResolveReviewDir`, `IterationDir`, atomic write helpers
- `internal/state/gate.go` (New) — `Gate` struct with explicit `json:"name"` tags, custom `MarshalJSON` delegating to `jsonutil.MarshalStable`, `Iteration`, `GateRef`, `Consensus`, `Decision`, `ReviewerOut` types — schema preserved verbatim
- `internal/state/lock.go` (New) — `WithFileLock(ctx, lockPath, timeout, fn)` wrapping `gofrs/flock` (5s default timeout matching bash)
- `internal/state/atomic.go` (New) — `AtomicWrite` mirroring `bin/telemetry-lib.sh`'s `atomic_write` (tmp + rename)
- `internal/state/store_test.go` (New) — gate load/save, atomic write, malformed JSON
- `internal/state/lock_interop_test.go` (New) — Go gofrs/flock holder vs bash `flock(1)` holder mutual exclusion test (Linux + macOS)

### Depends

- T001, T005 (`MarshalStable` for `Gate.MarshalJSON`)

### Goal

Provide gate-state.json read/write/lock plumbing whose on-disk byte shape and lock semantics match bash exactly, so Phase 1+2 bash bridges can interoperate on the same path. Skeleton in this task; full active-gate lookup, iteration save/load, and state-machine transitions land in T012 (Phase 1).

### Implementation Notes

- **Cross-runtime flock interop is load-bearing**. `gofrs/flock` wraps `flock(2)` on BSD/Darwin and `fcntl(F_SETLK)` on Linux; both are compatible with `flock(1)` PROVIDED both sides lock the same file path. Path normalization centralized here — use `filepath.EvalSymlinks` consistently to avoid realpath-vs-symlink mismatches.
- 5-second default lock timeout matches bash's `flock -w 5`.
- `Gate` struct uses explicit `json:"name"` tags matching every existing key in current bash output. Custom `MarshalJSON` delegates to `jsonutil.MarshalStable` (T005) so key order matches captured goldens.
- **Lock-deadlock rule**: Go must release its own `gate-state.json` lock BEFORE invoking the bash debate bridge in T015. This rule is enforced architecturally via `WithFileLock` scoping; callers that hold a lock across an exec of the bridge wrapper are bugs.
- Integration test spawns `flock /tmp/test-state.json sleep 5 &` from Go test, then asserts `Locker.Lock(ctx, 6s)` blocks until the bash child exits.
- Skeleton scope: types, signatures, atomic-write, lock-interop. Active-gate lookup, status transitions (`pending → awaiting_decision`, `pending → resolved` PASS, `pending → resolved` manual, `pending → resolved` defensive ERROR fallback per `README.md:317–321`), and iteration lifecycle land in T012.

### Verification

- `go test ./internal/state/... -race -cover` green.
- `lock_interop_test.go` passes on both Linux and macOS in CI: `flock(1)` shell child holds the lock; Go `Locker.Lock` blocks for ~5s until the child exits; no torn writes when N writers contend.
- Negative test: opening a malformed `gate-state.json` returns an error rather than silently producing an empty `Gate`.
- `Gate` round-trip (Marshal → Unmarshal → Marshal) is byte-stable.

---

## T008 — Foundation: internal/telemetry skeleton (types + signatures)
```meta
phase: Foundation
files: [internal/telemetry/telemetry.go, internal/telemetry/types.go, internal/telemetry/telemetry_test.go]
depends: [T001, T005, T007]
acceptance: [AC2]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L450-L485
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L450-L485](2026-04-30-port-shell-to-go-plan.md#L450-L485) — Section: Technical Design — internal/telemetry
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L62](2026-04-30-port-shell-to-go-plan.md#L62) — Section: Testing Constraints — telemetry extractor parity
- Spec: N/A

### Files

- `internal/telemetry/types.go` (New) — `Agent` enum, `AgentTelemetry`, `Summary`, `RunPatch` types matching bash's `extract_*_telemetry` output shape
- `internal/telemetry/telemetry.go` (New) — function signatures `InitIterationDir`, `ExtractClaude`, `ExtractCodexJSONL`, `ExtractGemini`, `ExtractFromJSON`, `ExtractFromJSONL`, `WriteAgentTelemetry`, `UpdateRunTelemetry`, `UpdateRunTelemetryOnResolve`, `SummaryForOutput` — bodies return `nil` / not-implemented stub for now
- `internal/telemetry/telemetry_test.go` (New) — compile-only smoke tests asserting signatures exist; full extractor parity tests in T018

### Depends

- T001, T005 (jsonutil for ordered marshal of telemetry JSON), T007 (state.Store dependency)

### Goal

Provide the type surface and function signatures consumers in Phase 1 (cmd/review-gate) and Phase 2 (hooks, generator) can compile against. Full extractor implementation lands in T018 (Phase 2 — extractors against the 26 fixtures + iteration lifecycle).

### Implementation Notes

- This is a **skeleton task** by design: it exists so cmd/review-gate (Phase 1) can wire up the package without the full extractor logic. The telemetry extractor parity test (behavioral checklist item #4) lands when extractors are implemented in T018.
- `AgentTelemetry` field set must be derived from current bash output via goldens captured in T009 — do not invent new fields.
- Stubs MUST return a clear "not implemented" error (or `errors.ErrUnsupported`) rather than silently returning zero values, so any caller in Phase 1 that accidentally exercises extractor code fails loudly.
- No new persisted state backend introduced; this package writes per-reviewer telemetry JSON into `iteration-N/` and patches `gate-state.json` via the `Store`.

### Verification

- `go vet ./internal/telemetry/...` clean.
- Compile-only smoke test asserts every public function signature exists.
- Calling `ExtractClaude(nil)` returns the stub error (proves the package fails loudly, not silently).

---

## T009 — Foundation: 24-cell golden fixture matrix capture + driver + bash test runner
```meta
phase: Foundation
files: [bin/tests/capture-goldens.sh, bin/tests/run-all.sh, bin/tests/fixtures/golden/.gitkeep, bin/tests/golden/normalize.sh]
depends: [T001, T002, T005]
acceptance: [AC2, AC4]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L191-L192
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L191-L192](2026-04-30-port-shell-to-go-plan.md#L191-L192) — Section: Prerequisites — golden capture
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L1003-L1019](2026-04-30-port-shell-to-go-plan.md#L1003-L1019) — Section: Testing — Golden fixture normalization
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L65](2026-04-30-port-shell-to-go-plan.md#L65) — Section: Testing Constraints — 24-cell golden parity
- Spec: N/A

### Files

- `bin/tests/capture-goldens.sh` (New, ~150 LOC) — driver that runs unmodified bash production across `{fast,smart,max} × {single,debate} × {code,plan,spec,ask}` = 24 cells; stores stdout/stderr/exit/`gate-state.json`/`iteration-N/` tree per cell under `bin/tests/fixtures/golden/{mode}/{path}/{kind}/`
- `bin/tests/run-all.sh` (New) — stable CI runner for existing bash acceptance tests; iterates `test-*.sh`, captures pass/fail, exits non-zero on any failure
- `bin/tests/golden/normalize.sh` (New) — wraps a real bash production run and writes the canonicalized output to `bin/tests/fixtures/golden/<cell>/canonical.json` using the same masks `internal/jsonutil.Canonicalize` applies
- `bin/tests/fixtures/golden/.gitkeep` (New) — ensure the directory is tracked

### Depends

- T001 (Makefile targets), T002 (`.bash-prod` files exist for capture), T005 (`internal/jsonutil.Canonicalize` defines the mask list normalize.sh mirrors)

### Goal

Capture the 24-cell baseline from unmodified bash production BEFORE replacing any production script. This is the **load-bearing prerequisite** for every later phase's golden diff release-gate. Reviewer outputs are stubbed via `bin/tests/fixtures/` replay so capture is deterministic.

### Implementation Notes

- Capture against the renamed `.bash-prod` files (T002) directly; the compat-wrappers also work but are unnecessary indirection during capture.
- Stub reviewer CLIs via the existing fixture-replay mechanism in `bin/tests/fixtures/` — `claude-output*.json`, `codex-output.jsonl`, `gemini-output*.txt`. Capture must produce identical results across runs (deterministic).
- Each cell stores: `stdout`, `stderr`, exit code, final `gate-state.json`, `iteration-N/` tree (drafts, telemetry, raw, report.md), per-reviewer telemetry.
- `make refresh-goldens` Makefile target re-runs capture through `normalize.sh` when an intentional behavior change lands; the diff that lands in the PR is the only place the team approves new bash output as canonical.
- **The normalization layer is never tweaked to silence a real diff** — that's the anti-pattern call-out from the plan.
- `run-all.sh` must surface individual test failures (don't swallow), set non-zero exit on any failure, and respect `set -euo pipefail`.

### Verification

- `bash bin/tests/capture-goldens.sh` populates all 24 cells under `bin/tests/fixtures/golden/`; second run produces byte-identical output (determinism check).
- `bash bin/tests/run-all.sh` runs the existing bash test corpus and exits non-zero on first failure.
- A Go test (e.g. `internal/jsonutil/normalize_shell_equivalence_test.go`) shells out to `bash bin/tests/golden/normalize.sh` over a representative input and asserts byte-equality vs `internal/jsonutil.Canonicalize(input, DefaultCanonicalOpts())` over the same input. Run via `go test ./internal/jsonutil/ -run TestNormalizeShellEquivalence`. The shell driver and the Go function MUST stay in lockstep; this test fails on any divergence (golden-diff release-gate depends on this equivalence).
- Capture-time test: deliberately introduce a timestamp drift in a fixture and assert `normalize.sh` masks it to `<TIMESTAMP>`.

---

## T010 — Foundation: Wire CI workflow + bench driver (build + Go tests + bash acceptance + golden diff + bench)
```meta
phase: Foundation
files: [.github/workflows/test.yml, bench/driver.go, bench/driver_test.go]
depends: [T001, T004, T005, T006, T007, T008, T009]
acceptance: [AC4, AC14]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L736-L754
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L736-L754](2026-04-30-port-shell-to-go-plan.md#L736-L754) — Section: CI design
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L57-L76](2026-04-30-port-shell-to-go-plan.md#L57-L76) — Section: Testing Constraints — 10-class behavioral checklist
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L990](2026-04-30-port-shell-to-go-plan.md#L990) — Section: Risks — Pattern-rule precedence assertion
- Spec: N/A

### Files

- `.github/workflows/test.yml` (Modify; skeleton from T001) — full matrix `{ubuntu-latest, macos-latest} × {go: 1.25.x}`; steps: checkout → setup-go (cache) → install transitional jq + GNU coreutils/timeout for bash tests → install reviewer-CLI stubs → `make install-binaries` (verify `test -x` for every shim and dist artifact) → `go test ./... -race -cover -coverprofile=coverage.out` (printed but not gated) → `bash bin/tests/run-all.sh` → `make golden-diff` → `make bench` (warning-only) → CI assertion (incremental per shipped phase): an explicit per-phase list (Phase 0 = empty no-op; Phase 1 = `dist/review-gate`; Phase 2 adds the six lifecycle/generator binaries; Phase 3 adds `dist/cerberus-amp-toolbox`) is grepped: `for c in $PHASE_LIVE_BINARIES; do ! grep -q '^# cerberus dist compat-wrapper v1' "dist/$c" || { echo "compat-wrapper found in shipped artifact dist/$c" >&2; exit 1; }; done`. The variable is defined in the workflow file itself (a literal list per the active phase), NOT a Makefile-derived name.

### Files

- `.github/workflows/test.yml` (Modify; skeleton from T001) — see above for full step list
- `bench/driver.go` (New) — small Go driver invoking each `dist/<name> --help` 100× cold via `os/exec`, recording p50/p95 wall-clock per binary; emits machine-readable JSON to stdout for CI artifact upload
- `bench/driver_test.go` (New) — smoke test asserting the driver runs without panicking against `dist/review-gate` once Phase 1 ships (skipped before that)

### Depends

- T001 (workflow skeleton + Makefile), T004-T008 (Go packages exist so tests run), T009 (golden capture + run-all.sh)

### Goal

Land the full CI workflow that runs the load-bearing 10-class behavioral checklist on every push/PR across Linux + macOS. Also land the `bench/` driver source files referenced by the `make bench` target. Coverage percentage is printed but **not** a release gate.

### Implementation Notes

- Triggers: `push` to any branch, `pull_request`, `workflow_dispatch`.
- `actions/setup-go@v5` with cache for modules + build cache (mitigates 8-binary build cost).
- Build step: `make install-binaries` (which builds per cmd into `dist.new/` and atomically swaps to `dist/`); verify `test -x` for both every canonical shim and every produced `dist/<name>`.
- Coverage: `go test ./... -race -cover -coverprofile=coverage.out` — print to CI output as supporting evidence; **no minimum-percentage gate** because line coverage is gameable.
- The behavioral checklist (Testing Constraints) is the load-bearing gate; per-phase release gate runs the relevant subset.
- Pattern-rule precedence assertion (Risks > Pattern-rule precedence): after `make install-binaries`, grep each `dist/<name>` for the `cerberus dist compat-wrapper v1` sentinel and assert NONE match for binaries whose phase has shipped. Phase 0 covers no Go-built artifacts yet; Phase 1 adds `dist/review-gate`; Phase 2 adds the six lifecycle/generator binaries; Phase 3 adds `dist/cerberus-amp-toolbox` and effectively the entire `GO_CMDS` list.
- Artifact upload: golden diff output uploaded on failure for forensic review.
- Build cost target ~5 minutes per matrix cell; mitigate via cached Go modules + shared build cache.
- `bench/driver.go` is the single concrete deliverable behind `make bench`; the Makefile target `bench` (declared in T001) shells out to `go run ./bench` against `dist/`. The hard `≤ bash + 10ms` budget remains warning-only initially per plan §Testing & Validation L1041; the driver records baselines that a future task may convert to a gate.

### Verification

- A trivial PR triggers the workflow; both `ubuntu-latest` and `macos-latest` cells go green.
- Race detector clean.
- `go test -cover` percentage appears in workflow output (not gated).
- `make golden-diff` exits non-zero on intentional drift (verified by deliberately modifying a fixture).
- Pattern-rule precedence assertion fires loudly when the compat-wrapper rule incorrectly takes precedence (test by stubbing a `cmd/review-gate/main.go` and observing the assertion).

---

## T011 — [integration-path-test] System Wiring: cmd/review-gate skeleton + internal/state complete + internal/host + integration test via shim
```meta
phase: Phase 1 — Core review-gate
files: [cmd/review-gate/main.go, cmd/review-gate/dispatch.go, cmd/review-gate/integration_test.go, internal/state/active.go, internal/state/iteration.go, internal/state/transitions.go, internal/state/state_machine_test.go, internal/host/host.go, internal/host/host_test.go]
depends: [T002, T003, T004, T006, T007, T009, T010]
acceptance: [AC2, AC6]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L690-L734
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L690-L734](2026-04-30-port-shell-to-go-plan.md#L690-L734) — Section: Phase 1 cutover plan
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L223-L224](2026-04-30-port-shell-to-go-plan.md#L223-L224) — Section: High-Level Approach — Phase 1
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L60](2026-04-30-port-shell-to-go-plan.md#L60) — Section: Testing Constraints — state machine transitions
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L667-L688](2026-04-30-port-shell-to-go-plan.md#L667-L688) — Section: cmd/<name>/main.go wiring
- Spec: N/A

### Files

- `cmd/review-gate/main.go` (New) — entry point: `signal.NotifyContext`, `cerberusenv.Resolve`, dispatch to subcommands via stdlib `flag` + manual switch
- `cmd/review-gate/dispatch.go` (New) — subcommand router; subcommands return stubs (NotImplemented) except `--version`/`--help` until T014/T015
- `cmd/review-gate/integration_test.go` (New) — failing end-to-end test: runs `dist/review-gate status` against a fixture review dir and asserts byte-equal stdout vs the captured Phase 0 golden
- `internal/state/active.go` (New) — `FindActiveGate`, `LoadGate`, `SaveGate`, `UpdateGate`, `ArchiveReviews` (completes the T007 skeleton)
- `internal/state/iteration.go` (New) — `LoadIteration`, `SaveIteration`, `IterationDir`
- `internal/state/transitions.go` (New) — documented status transitions (`pending → awaiting_decision`, `pending → resolved` PASS, manual resolve, defensive ERROR fallback per `README.md:317–321`); active-gate guard treats `resolved` as not-active
- `internal/state/state_machine_test.go` (New) — table-driven tests for every transition + active-gate guard
- `internal/host/host.go` (New) — per-host adapter glue; default behaviors for claude/codex/amp/generic
- `internal/host/host_test.go` (New) — tests for adapter selection from `Env.Host`

### Depends

- T002, T003 (shims + bridge wrapper), T004 (cerberusenv), T006 (procgroup), T007 (state skeleton), T009 (goldens to diff against), T010 (CI runs the integration test)

### Goal

Establish the **integration-path-test for Phase 1's feature** (cmd/review-gate). Skeleton + integration test combined: project compiles, integration test runs end-to-end through `cmd/review-gate` → `internal/state` → `internal/host`, and FAILS for the right reason (subcommand bodies return NotImplemented) — proving the wiring path is correct before T012-T015 fill in behavior.

### Implementation Notes

- This is the **integration-path-test task** for Phase 1 — exactly one task per feature MUST include `[integration-path-test]` in the title.
- Skeleton + integration test combined per the TDD ordering rule (you can't write the integration test without the skeleton).
- Subcommand list (preserved verbatim from `bin/review-gate.bash-prod`): `check`, `spawn`, `spawn-code-review`, `spawn-plan-review`, `spawn-spec-review`, `spawn-epic-verify`, `spawn-ask`, `wait`, `resolve`, `artifact-path`, `author-context`, `status`. All keep their flag set, exit codes, and output formats.
- Stdlib `flag` + manual switch per the "Pattern to follow" constraint; no third-party CLI framework.
- State-machine test covers all five transitions in the behavioral checklist (item #2): `pending → awaiting_decision` (debate degraded), `pending → resolved` PASS, manual resolve via `bin/review-gate resolve`, defensive ERROR fallback (`consensus.verdict=ERROR` per `README.md:317–321`), and the active-gate guard.
- Integration test runs against a fixture review dir captured by T009; uses the `Canonicalize` mask before byte comparison.
- Wiring map: `os.Args → flag.Parse → dispatch → subcommand handler → state.Store → cerberusenv.Env → host.Adapter`.

### Verification

- `make install-binaries` produces `dist/review-gate` (real Go binary, not compat-wrapper).
- `dist/review-gate --help` prints the same top-level help text as `bash bin/review-gate.bash-prod --help` (byte-identical after `Canonicalize`).
- Integration test FAILS with "NotImplemented" for each unimplemented subcommand (proves the wiring path is exercised).
- `go test ./internal/state/... -race -cover` green; state-machine table tests cover all 5 transitions.
- Pattern-rule precedence assertion (T010) confirms `dist/review-gate` does NOT contain the compat-wrapper sentinel.

---

## T012 — internal/reviewers (argv assembly + invocation + per-(agent,mode) goldens)
```meta
phase: Phase 1 — Core review-gate
files: [internal/reviewers/reviewers.go, internal/reviewers/models.go, internal/reviewers/claude.go, internal/reviewers/codex.go, internal/reviewers/gemini.go, internal/reviewers/runner.go, internal/reviewers/argv_golden_test.go, internal/reviewers/runner_test.go, bin/tests/fixtures/golden/argv/.gitkeep]
depends: [T011]
acceptance: [AC6]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L524-L566
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L524-L566](2026-04-30-port-shell-to-go-plan.md#L524-L566) — Section: Technical Design — internal/reviewers
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L61](2026-04-30-port-shell-to-go-plan.md#L61) — Section: Testing Constraints — reviewer argv goldens
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L1095](2026-04-30-port-shell-to-go-plan.md#L1095) — Section: Open Questions — OQ-C reviewer argv exact-match
- Spec: N/A

### Files

- `internal/reviewers/reviewers.go` (New) — types (`Mode`, `ReviewType`, `ReviewerName`, `Request`, `Result`, `Runner` interface)
- `internal/reviewers/models.go` (New) — `ModeConfig`, `ModeSettings`; per-mode model selection table (fast/smart/max)
- `internal/reviewers/claude.go` (New) — `BuildClaudeCommand`
- `internal/reviewers/codex.go` (New) — `BuildCodexCommand`
- `internal/reviewers/gemini.go` (New) — `BuildGeminiCommand`
- `internal/reviewers/runner.go` (New) — `Runner` impl using `os/exec` + `procgroup.Detach` for spawning, `Available` check via `LookPath`
- `internal/reviewers/argv_golden_test.go` (New) — table-driven golden test diffing rendered argv against `bin/tests/fixtures/golden/argv/<reviewer>/<mode>/<type>.json`
- `internal/reviewers/runner_test.go` (New) — fake-CLI tests for spawn/timeout/detach
- `bin/tests/fixtures/golden/argv/.gitkeep` (New) — directory for captured per-(agent,mode,type) argv goldens (captured from bash in this task)

### Depends

- T011 (state + procgroup + cerberusenv wired)

### Goal

Port `bin/review-gate-models.sh` argv assembly into a Go package. Reviewer CLIs (`claude`, `codex`, `gemini`) remain external subprocesses; argv must match bash byte-for-byte, gated by per-(agent,mode,type) goldens captured in this task.

### Implementation Notes

- TDD: write failing argv golden tests first (using captured bash argv), then implement builders to make them pass.
- Capture bash argv: temporarily insert `printf '%s\n' "$@" > /tmp/argv-snapshot` shim into `bin/review-gate-models.sh.bash-prod` invocations on a feature branch, snapshot per cell, commit goldens.
- Argv captured as a `[]string` slice (NOT a flat command string) and diffed against JSON goldens.
- Per the plan: `claude`, `codex`, `gemini` argv strings assembled by Go must match bash byte-for-byte (whitespace, `--` separator placement, quoted-arg policy).
- Spawn uses `procgroup.Detach` (T006) so reviewer subprocesses survive parent exit; caller invokes `Process.Release()`.
- This task implements behavior under T011's integration test (T011's `[integration-path-test]` feature includes spawn paths once T012 is in).

### Verification

- `go test ./internal/reviewers/... -race -cover` green.
- Argv goldens cover the 24-cell matrix's reviewer invocations (per `(agent, mode, type)`).
- Negative test: an unknown mode returns an error, not a default fallback.
- Existing `bin/tests/test-codex-review-invocation.sh` and `test-review-gate-gemini-policy.sh` continue to pass.

---

## T013 — internal/prompts (markdown loader from prompts/)
```meta
phase: Phase 1 — Core review-gate
files: [internal/prompts/renderer.go, internal/prompts/data.go, internal/prompts/renderer_test.go]
depends: [T011]
acceptance: [AC4]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L641-L653
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L641-L653](2026-04-30-port-shell-to-go-plan.md#L641-L653) — Section: Technical Design — internal/prompts
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L26](2026-04-30-port-shell-to-go-plan.md#L26) — Section: Scope — prompts/ unchanged
- Spec: N/A

### Files

- `internal/prompts/renderer.go` (New) — `Renderer{Root}`, `Load(path)`, `RenderReviewPrompt(type, data)`, `RenderDebatePrompt(type, data)`
- `internal/prompts/data.go` (New) — `PromptData`, `DebatePromptData` types
- `internal/prompts/renderer_test.go` (New) — load + render tests against existing `prompts/*.md`

### Depends

- T011

### Goal

Provide a runtime markdown template loader for `prompts/*.md` that the review-gate subcommands can call. Prompts themselves are NOT rewritten — `prompts/` is unchanged per the Scope section.

### Implementation Notes

- TDD: write a failing test that asserts `Load("prompts/code-review.md")` returns the file's bytes; then implement.
- No template engine introduced; the bash side currently does string substitution. Mirror that behavior with `text/template` only if the bash side currently uses braces. Otherwise plain `os.ReadFile` is correct.
- Negative test: loading a missing prompt path returns a clear error naming the path.

### Verification

- `go test ./internal/prompts/... -race -cover` green.
- Loaded content for each existing `prompts/*.md` is byte-identical to `os.ReadFile` of the same path (no transformation drift).

---

## T014 — cmd/review-gate non-debate subcommands implementation (check/spawn-*/wait/resolve/artifact-path/author-context/status)
```meta
phase: Phase 1 — Core review-gate
files: [cmd/review-gate/check.go, cmd/review-gate/spawn.go, cmd/review-gate/wait.go, cmd/review-gate/resolve.go, cmd/review-gate/artifact_path.go, cmd/review-gate/author_context.go, cmd/review-gate/status.go, cmd/review-gate/check_test.go, cmd/review-gate/spawn_test.go]
depends: [T011, T012, T013]
acceptance: [AC1, AC4, AC10]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L887-L896
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L887-L896](2026-04-30-port-shell-to-go-plan.md#L887-L896) — Section: API/Interface Design
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L693-L697](2026-04-30-port-shell-to-go-plan.md#L693-L697) — Section: Phase 1 cutover plan — subcommand dispatch
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L889](2026-04-30-port-shell-to-go-plan.md#L889) — Section: API/Interface Design — review-gate exit codes per README:458–464
- Spec: N/A

### Files

- `cmd/review-gate/check.go` (New) — Claude `Stop` check loop (collapses `bin/review-gate-hook.sh` logic)
- `cmd/review-gate/spawn.go` (New) — `spawn`, `spawn-code-review`, `spawn-plan-review`, `spawn-spec-review`, `spawn-epic-verify`, `spawn-ask`; uses `internal/reviewers` + `internal/prompts`; runs reviewers detached via `procgroup.Detach`
- `cmd/review-gate/wait.go` (New) — `wait` subcommand with `--json` flag
- `cmd/review-gate/resolve.go` (New) — manual resolve subcommand (`pending → resolved` transition)
- `cmd/review-gate/artifact_path.go` (New) — `artifact-path` subcommand
- `cmd/review-gate/author_context.go` (New) — `author-context` subcommand
- `cmd/review-gate/status.go` (New) — `status` subcommand
- `cmd/review-gate/check_test.go`, `cmd/review-gate/spawn_test.go` (New) — fixture-driven tests

### Depends

- T011, T012, T013

### Goal

Make the 12 non-debate cells of the golden matrix pass byte-equal by implementing all non-debate subcommands. After this task, `dist/review-gate` is a real Go binary handling every subcommand except `--debate`.

### Implementation Notes

- Each subcommand goes through `state.WithFileLock(env.GateStatePath()+".lock", 5*time.Second, …)` for any read-modify-write of `gate-state.json` (lock-deadlock rule from T007).
- Exit codes preserved per `README.md` lines 458–464 (review-gate 0–4) and `bin/review-gate.bash-prod` headers; tests assert exact codes.
- Within-task TDD: write failing unit tests per subcommand, then implement; ensure T011's integration test goes green for non-debate paths.
- Hooks-stdin/stdout discipline: hooks must not leak chatter. Use a single `internal/host.Logger` that defaults to `io.Discard` for non-error messages and writes structured JSON to stderr only when `CERBERUS_DEBUG=1`.
- Trap parity: `defer os.Remove(markerPath)` plus a signal handler that `select`-receives SIGINT/SIGTERM and runs cleanup before exit (matches bash ERR/EXIT traps).
- After this task lands, the 12 non-debate cells of the golden matrix should be byte-equal in `make golden-diff`.

### Verification

- `go test ./cmd/review-gate/... -race -cover` green.
- 12 non-debate golden cells from `bin/tests/fixtures/golden/` are byte-equal after `Canonicalize`.
- All `bin/tests/test-review-gate-*.sh` (except the ones that touch `--debate`) pass against `dist/review-gate`.
- T011 integration test goes from FAIL to PASS for non-debate paths.
- `test-review-gate-spawn-no-setsid.sh` adjusted to inspect process-group state via `ps -o pgid` rather than literal `setsid` invocation (per Risks > reviewer subprocess detach).
- Negative test: `dist/review-gate spawn --mode invalid` exits non-zero with a clear error rather than silently defaulting.

---

## T015 — cmd/review-gate --debate Supervise bridge to bin/review-gate-debate-bridge.sh
```meta
phase: Phase 1 — Core review-gate
files: [cmd/review-gate/debate_bridge.go, cmd/review-gate/debate_bridge_test.go, internal/cerberusenv/export_test.go]
depends: [T002, T006, T011, T012, T014]
acceptance: [AC6, AC7, AC8]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L697-L734
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L697-L734](2026-04-30-port-shell-to-go-plan.md#L697-L734) — Section: Phase 1 cutover plan — bash debate bridge (Supervise pattern)
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L968-L971](2026-04-30-port-shell-to-go-plan.md#L968-L971) — Section: Risks — flock interop, lock deadlock at debate bridge
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L1080-L1081](2026-04-30-port-shell-to-go-plan.md#L1080-L1081) — Section: Deviation Log — Supervise pattern (Go parent + bash child)
- Spec: N/A

### Files

- `cmd/review-gate/debate_bridge.go` (New) — `--debate` handler: assemble env via `cerberusenv.ExportNeutralEnv` + `PATH`/`HOME`/`TMPDIR`/`LANG` + reviewer API keys; release Go's `gate-state.json` lock first; exec `bash bin/review-gate-debate-bridge.sh` with `procgroup.Supervise`; `signal.Notify` forwarder delivers SIGINT/SIGTERM/SIGHUP/SIGQUIT/SIGUSR1 to child pgid; propagate child exit code via `cmd.ProcessState.ExitCode()`
- `cmd/review-gate/debate_bridge_test.go` (New) — integration test asserting (a) all required env vars cross the bash boundary; (b) signal forwarding delivers to child pgid; (c) exit code propagated verbatim; (d) Go `defer`s run normally (lock release, marker file cleanup)
- `internal/cerberusenv/export_test.go` (New) — env audit test: execs `bash -c 'env | grep -E "^(CERBERUS|REVIEW_GATE|CLAUDE)_"'` from Go and asserts the expected set (covers OQ-D)

### Depends

- T011, T012, T014, T006 (procgroup.Supervise)

### Goal

Wire `--debate` to the Phase 1 bash bridge using the Supervise pattern: Go is the parent, owns signal forwarding, releases its own state lock before exec, and propagates the bash child's exit code verbatim. State exchange remains via `gate-state.json` with cross-runtime flock interop holding.

### Implementation Notes

- **Supervise pattern, NOT syscall.Exec**. Go remains the parent so `defer`-driven cleanup (lock release, marker file removal) runs normally and Go has a single signal-forwarding seam. `syscall.Exec` was rejected because it skips `defer`s and forfeits cleanup on signal (Deviation Log row "Bash sole-process model on `--debate`").
- Env assembly via `cerberusenv.ExportNeutralEnv` (T004): full `CERBERUS_*` + `REVIEW_GATE_*` + `CLAUDE_*` aliases; plus `PATH`/`HOME`/`TMPDIR`/`LANG`; plus reviewer API keys (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `GEMINI_API_KEY` if present in parent env).
- `cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}` (Supervise pattern from T006).
- Lock-deadlock rule: Go MUST release its own `gate-state.json` lock BEFORE invoking the bridge. Architecturally enforced via `state.WithFileLock` scoping; integration test runs `review-gate --debate` end-to-end and asserts no Go-side lock is held across the exec.
- Cross-runtime flock interop: bash child acquires its own locks via `flock(1)`; Go's `gofrs/flock` and bash `flock(1)` cooperate on the same path. Verified by `internal/state/lock_interop_test.go` (T007) plus the end-to-end test here.
- Exit-code propagation: `cmd.Wait` returns `*exec.ExitError`; forward `cmd.ProcessState.ExitCode()` verbatim. Debate exit codes 5 (aggregator failure), 6 (preflight or mid-debate degraded), 130 (cancellation) MUST round-trip correctly per `README.md` lines 304–321.

### Verification

- `go test ./cmd/review-gate/... -run TestDebateBridge -race` green on both Linux and macOS.
- End-to-end test: SIGTERM to Go parent → bash child receives via pgid → child cleanup runs → exit code propagated.
- Env audit test: snapshot bash-side env equals expected set (OQ-D); no leakage of unrelated parent env into bash subprocess that would change behavior.
- Lock-deadlock test: `review-gate --debate` end-to-end run shows no Go-side lock held during bash bridge execution (instrument `state.WithFileLock` to log enter/exit).
- Manual smoke: `bin/review-gate spawn-code-review --debate --max-rounds=1` against a real workspace produces output indistinguishable from the all-bash predecessor (excluding `ps`/`pgrep` which now shows Go parent + bash child).

---

## T016 — Phase 1 release gate: delete bin/review-gate.bash-prod, run 12 non-debate goldens + bash acceptance + manual smoke
```meta
phase: Phase 1 — Core review-gate
files: [bin/review-gate.bash-prod]
depends: [T002, T012, T013, T014, T015]
acceptance: [AC4, AC5, AC14]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L223-L224
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L223-L224](2026-04-30-port-shell-to-go-plan.md#L223-L224) — Section: High-Level Approach — Phase 1 release gate
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L734](2026-04-30-port-shell-to-go-plan.md#L734) — Section: Phase 1 cutover plan — release gate
- Spec: N/A

### Files

- `bin/review-gate.bash-prod` (Delete from git) — legacy 5767-LOC bash production removed; rollback = `git revert` of the phase commit which restores it

### Depends

- T012, T013, T014, T015 (full Phase 1 implementation)

### Goal

Ship Phase 1 as a tagged release. Hard cutover: legacy bash production deleted; canonical shim path unchanged; rollback via `git revert` + version pin to prior tag.

### Implementation Notes

- The committed `bin/review-gate` shim is **unchanged** by this task. The cutover happens entirely inside `dist/`, which now holds `dist/review-gate` as a real Go binary.
- Tag the release after CI green. Document rollback procedure in the release notes.
- Phase 1 release gate (load-bearing checklist subset):
  1. 12 non-debate golden cells byte-equal (`make golden-diff`).
  2. Every external-surface `bin/tests/test-*.sh` passes against `dist/review-gate`.
  3. Cross-runtime flock interop test green on Linux + macOS (T007).
  4. Procgroup detach + supervise tests green (T006).
  5. Reviewer argv goldens green (T012).
  6. Manual smoke on Claude `Stop` hook against a real workspace + `wait --json` against a live debate (still bash via bridge).
- After this task ships, `dist/review-gate` MUST NOT match the `cerberus dist compat-wrapper v1` sentinel (CI assertion from T010).

### Verification

- CI matrix green on `ubuntu-latest` + `macos-latest`.
- `make golden-diff` green for the 12 non-debate cells.
- `bash bin/tests/run-all.sh` green.
- Manual smoke transcript captured in the release notes.
- Pattern-rule precedence assertion: `! grep -q '^# cerberus dist compat-wrapper v1' dist/review-gate`.
- Tag pushed: `git tag -a v$N -m "Phase 1 — Core review-gate ported to Go"`.
- Negative case: documented one-line rollback (`git revert <phase-commit>`) restores `bin/review-gate.bash-prod` and CI returns to all-bash mode green.

---

## T017 — [integration-path-test] System Wiring: internal/hooks skeleton + cmd/claude-session-init + integration via fixture replay
```meta
phase: Phase 2 — Hooks + generator
files: [internal/hooks/hooks.go, internal/hooks/claude_session_init.go, internal/hooks/hooks_test.go, cmd/claude-session-init/main.go, cmd/claude-session-init/integration_test.go]
depends: [T011]
acceptance: [AC10]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L605-L615
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L605-L615](2026-04-30-port-shell-to-go-plan.md#L605-L615) — Section: Technical Design — internal/hooks
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L66](2026-04-30-port-shell-to-go-plan.md#L66) — Section: Testing Constraints — hook fixture replay
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L225](2026-04-30-port-shell-to-go-plan.md#L225) — Section: High-Level Approach — Phase 2
- Spec: N/A

### Files

- `internal/hooks/hooks.go` (New) — types + signatures: `HandleClaudeSessionInit`, `HandleCodexSessionInit`, `HandleCodexStop`, `HandleTaskCompleted`, `HandleTeammateIdle`
- `internal/hooks/claude_session_init.go` (New) — Claude SessionStart implementation (smallest hook, 25 LOC bash equivalent)
- `internal/hooks/hooks_test.go` (New) — fixture-driven tests against `bin/tests/fixtures/claude-output*.json`
- `cmd/claude-session-init/main.go` (New) — thin entrypoint per the canonical `cmd/<name>/main.go` pattern
- `cmd/claude-session-init/integration_test.go` (New) — failing end-to-end test asserting byte-equal stdout/stderr/exit-code vs the captured Phase 0 golden

### Depends

- T011 (state + cerberusenv + host wired)

### Goal

Establish the **integration-path-test for Phase 2's feature** (lifecycle hooks). Skeleton + integration test combined: `internal/hooks` compiles; `cmd/claude-session-init` runs end-to-end through fixture replay and FAILS for the right reason (other hook handlers stubbed) — proving the wiring path before T019-T021 fill in behavior.

### Implementation Notes

- `cmd/claude-session-init` is the smallest hook (25 LOC bash) — appropriate as the integration-path-test surface for the hook feature.
- Handler signature: `func HandleClaudeSessionInit(ctx, stdin, stdout, stderr, env) int` (returns exit code).
- Fixture replay: Go binaries are run against the JSON fixtures in `bin/tests/fixtures/` (`claude-output*.json`) and produce byte-equal stdout/stderr/exit-code to bash (behavioral checklist item #8).
- Hook timeouts cap end-to-end binary runtime: TaskCompleted = 2100s (`hooks/hooks.json` line 30), TeammateIdle = 10s (line 41). Go binaries set `context.WithTimeout` slightly under these budgets.
- Stdout/stderr discipline (Risks): hooks must not leak chatter. Use the `internal/host.Logger` that defaults to `io.Discard` for non-error messages.
- `gtimeout` exit-code mapping: `context.DeadlineExceeded` → exit 124 if any test asserts that specific code.

### Verification

- `make install-binaries` produces `dist/claude-session-init` (real Go binary).
- Integration test FAILS with NotImplemented for other hooks (proving the wiring).
- `cmd/claude-session-init` passes fixture replay against `bin/tests/fixtures/claude-output*.json`; byte-identical to bash output after `Canonicalize`.
- `bin/tests/test-host-neutral-state.sh` continues to pass.
- Pattern-rule precedence assertion confirms `dist/claude-session-init` is not a compat-wrapper.

---

## T018 — internal/telemetry complete: extractors for Claude/Codex/Gemini + iteration dir lifecycle
```meta
phase: Phase 2 — Hooks + generator
files: [internal/telemetry/extract_claude.go, internal/telemetry/extract_codex.go, internal/telemetry/extract_gemini.go, internal/telemetry/iteration.go, internal/telemetry/run.go, internal/telemetry/extract_test.go, internal/telemetry/iteration_test.go]
depends: [T008, T011]
acceptance: [AC2, AC4]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L450-L485
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L450-L485](2026-04-30-port-shell-to-go-plan.md#L450-L485) — Section: Technical Design — internal/telemetry
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L62](2026-04-30-port-shell-to-go-plan.md#L62) — Section: Testing Constraints — telemetry extractor parity (26 fixtures)
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L879-L880](2026-04-30-port-shell-to-go-plan.md#L879-L880) — Section: Data Model — iteration-N/ + per-reviewer telemetry JSON
- Spec: N/A

### Files

- `internal/telemetry/extract_claude.go` (New) — `ExtractClaude(data)` against `claude-output*.json`
- `internal/telemetry/extract_codex.go` (New) — `ExtractCodexJSONL(data)` against `codex-output.jsonl`
- `internal/telemetry/extract_gemini.go` (New) — `ExtractGemini(data)` against `gemini-output*.txt`
- `internal/telemetry/iteration.go` (New) — `InitIterationDir`, `WriteAgentTelemetry`
- `internal/telemetry/run.go` (New) — `UpdateRunTelemetry`, `UpdateRunTelemetryOnResolve`, `SummaryForOutput`
- `internal/telemetry/extract_test.go` (New) — table-driven tests against the 26 JSON fixtures
- `internal/telemetry/iteration_test.go` (New) — iteration dir lifecycle tests

### Depends

- T008 (skeleton), T011 (state.Store)

### Goal

Make the telemetry extractors produce byte-equal output to bash's `extract_*_telemetry` (`bin/telemetry-lib.sh:161–354`) against all 26 JSON fixtures. Iteration dir lifecycle (`init_iteration_dir`, `load_iteration`, `save_iteration`, `get_iteration_dir`) ports to Go.

### Implementation Notes

- TDD: write failing extractor tests against the existing 26 fixtures; implement until byte-equal.
- Per-reviewer telemetry JSON shape uses `jsonutil.MarshalStable` (T005) so key order matches captured goldens.
- `AgentTelemetry` field set comes from current bash output via the goldens captured in T009 — do not invent fields.
- iso8601 parse parity (Risks): Go's `time.Parse(time.RFC3339, …)` accepts the standard subset; bash currently shells to `gdate`/`date -u` which is more lenient. Port `test-iso8601-to-epoch.sh` (44 LOC) edge cases to Go and, if Go strictness diverges, fall back to a regex-driven parser in `internal/jsonutil`.
- Replaces the T008 stub returns; after this task lands, `internal/telemetry` is the single source of truth and the Phase 2 hook/generator binaries do not source `telemetry-lib.sh`.

### Verification

- `go test ./internal/telemetry/... -race -cover` green.
- All 26 JSON fixtures round-trip byte-equal through `Extract* → WriteAgentTelemetry`.
- iso8601 edge-case test ports the bash test cases and adds drift cases.
- `test-telemetry-unit.sh`, `test-telemetry-integration.sh`, `test-telemetry-e2e.sh` continue to pass.
- Negative test: feeding malformed JSONL returns an error rather than silently dropping rows.

---

## T019 — cmd/codex-session-init + cmd/codex-stop-hook implementation + fixture tests
```meta
phase: Phase 2 — Hooks + generator
files: [cmd/codex-session-init/main.go, cmd/codex-stop-hook/main.go, internal/hooks/codex_session_init.go, internal/hooks/codex_stop.go, internal/hooks/codex_test.go]
depends: [T017, T018]
acceptance: [AC10]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L605-L615
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L605-L615](2026-04-30-port-shell-to-go-plan.md#L605-L615) — Section: Technical Design — internal/hooks signatures
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L883-L884](2026-04-30-port-shell-to-go-plan.md#L883-L884) — Section: Data Model — Host/session registry + Codex session init
- Spec: N/A

### Files

- `cmd/codex-session-init/main.go` (New) — thin entrypoint
- `cmd/codex-stop-hook/main.go` (New) — thin entrypoint
- `internal/hooks/codex_session_init.go` (New) — `HandleCodexSessionInit` impl (196 LOC bash equivalent)
- `internal/hooks/codex_stop.go` (New) — `HandleCodexStop` impl (598 LOC bash equivalent)
- `internal/hooks/codex_test.go` (New) — fixture replay tests against `codex-output.jsonl`

### Depends

- T017 (hooks skeleton + integration test), T018 (telemetry extractors for Codex)

### Goal

Port the Codex SessionStart and Stop hooks to Go with byte-equal fixture-replay output. Codex Stop wait-knob semantics preserved.

### Implementation Notes

- TDD: failing fixture replay test, then implement.
- `gate-state.json` mutations go through `state.WithFileLock`.
- Stop-hook stdout/stderr discipline (Risks): hooks must not leak chatter to the host's stop-hook channel.
- Hook stdin payload shapes from existing fixtures (`codex-output.jsonl`); Go decoders use the same `json:"…"` tags.

### Verification

- `go test ./internal/hooks/... -race -cover` green.
- `dist/codex-session-init` and `dist/codex-stop-hook` produce byte-equal stdout/stderr/exit vs bash for every fixture.
- `bin/tests/test-codex-stop-hook.sh` and `test-codex-session-registry.sh` pass.

---

## T020 — cmd/cerberus-task-completed-hook implementation + fixture tests
```meta
phase: Phase 2 — Hooks + generator
files: [cmd/cerberus-task-completed-hook/main.go, internal/hooks/task_completed.go, internal/hooks/task_completed_test.go]
depends: [T017, T018]
acceptance: [AC10]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L605-L615
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L605-L615](2026-04-30-port-shell-to-go-plan.md#L605-L615) — Section: Technical Design — internal/hooks
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L55](2026-04-30-port-shell-to-go-plan.md#L55) — Section: Implementation Constraints — TaskCompleted timeout 2100s
- Spec: N/A

### Files

- `cmd/cerberus-task-completed-hook/main.go` (New) — thin entrypoint
- `internal/hooks/task_completed.go` (New) — `HandleTaskCompleted` impl (558 LOC bash equivalent)
- `internal/hooks/task_completed_test.go` (New) — fixture replay tests

### Depends

- T017, T018

### Goal

Port the TaskCompleted hook (the largest hook at 558 LOC bash) to Go with byte-equal fixture-replay output. 2100s timeout budget preserved via `context.WithTimeout`.

### Implementation Notes

- Set `context.WithTimeout` slightly under 2100s (e.g. 2090s) so the binary terminates internally before the host kills it, preserving the chance to write structured output on timeout.
- Use `internal/telemetry.UpdateRunTelemetry` and `state.WithFileLock` for `gate-state.json` updates.
- TDD: failing fixture replay, then implement.

### Verification

- `bin/tests/test-cerberus-task-completed-hook.sh` passes against `dist/cerberus-task-completed-hook`.
- Fixture replay byte-equal vs bash.
- Negative test: timeout fires at 2090s with exit code 124 (`gtimeout` parity from Risks).

---

## T021 — cmd/cerberus-teammate-idle-hook implementation + fixture tests
```meta
phase: Phase 2 — Hooks + generator
files: [cmd/cerberus-teammate-idle-hook/main.go, internal/hooks/teammate_idle.go, internal/hooks/teammate_idle_test.go]
depends: [T017, T018]
acceptance: [AC10]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L605-L615
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L605-L615](2026-04-30-port-shell-to-go-plan.md#L605-L615) — Section: Technical Design — internal/hooks
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L55](2026-04-30-port-shell-to-go-plan.md#L55) — Section: Implementation Constraints — TeammateIdle timeout 10s
- Spec: N/A

### Files

- `cmd/cerberus-teammate-idle-hook/main.go` (New) — thin entrypoint
- `internal/hooks/teammate_idle.go` (New) — `HandleTeammateIdle` impl (198 LOC bash equivalent)
- `internal/hooks/teammate_idle_test.go` (New) — fixture replay tests

### Depends

- T017, T018

### Goal

Port the TeammateIdle hook to Go with byte-equal fixture-replay output. 10s timeout budget preserved.

### Implementation Notes

- `context.WithTimeout(ctx, 9*time.Second)` (just under the 10s budget).
- Same hook-writing patterns as T020 (state lock, telemetry update).

### Verification

- `bin/tests/test-cerberus-teammate-idle-hook.sh` passes.
- Fixture replay byte-equal.

---

## T022 — [integration-path-test] cmd/generate + internal/generator + integration tests
```meta
phase: Phase 2 — Hooks + generator
files: [cmd/generate/main.go, internal/generator/generator.go, internal/generator/healthcheck.go, internal/generator/architecture_review.go, internal/generator/create_spec.go, internal/generator/create_plan.go, internal/generator/integration_test.go]
depends: [T011, T012, T013, T018]
acceptance: [AC4]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L617-L639
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L617-L639](2026-04-30-port-shell-to-go-plan.md#L617-L639) — Section: Technical Design — internal/generator
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L890](2026-04-30-port-shell-to-go-plan.md#L890) — Section: API/Interface Design — bin/generate actions
- Spec: N/A

### Files

- `cmd/generate/main.go` (New) — thin entrypoint
- `internal/generator/generator.go` (New) — `Action` enum + `Run(ctx, req, stdout, stderr)`
- `internal/generator/healthcheck.go` (New) — `healthcheck` action
- `internal/generator/architecture_review.go` (New) — `architecture-review` action
- `internal/generator/create_spec.go` (New) — `create-spec` action
- `internal/generator/create_plan.go` (New) — `create-plan` action
- `internal/generator/integration_test.go` (New) — failing integration test exercising end-to-end via cmd → generator → reviewers

### Depends

- T011 (state + cerberusenv + host), T012 (reviewers), T013 (prompts), T018 (telemetry complete)

### Goal

Port `bin/generate` (768 LOC bash) to Go preserving the four actions: healthcheck, architecture-review, create-spec, create-plan. This is the **integration-path-test for the generator feature**.

### Implementation Notes

- TDD: write failing integration test asserting generator produces byte-equal stdout/artifacts vs bash for a representative input across the four actions; then implement.
- Generator orchestrates `internal/reviewers` + `internal/prompts` + `internal/telemetry` + `internal/state`.
- By end of this task, `internal/telemetry` is the single source of truth and `bin/generate` does not source `telemetry-lib.sh`.

### Verification

- `dist/generate healthcheck` and the other three actions produce byte-equal output vs bash for representative inputs.
- Integration test passes (skeleton+integration combined was avoided here because T017 already established the hook integration path; for the generator, this single task combines integration + impl per the per-feature TDD rule).
- Bash acceptance tests touching `bin/generate` continue to pass.

---

## T023 — Phase 2 release gate: delete 6 lifecycle .bash-prod files + full hook smoke + verification
```meta
phase: Phase 2 — Hooks + generator
files: [bin/generate.bash-prod, bin/cerberus-task-completed-hook.bash-prod, bin/cerberus-teammate-idle-hook.bash-prod, bin/claude-session-init.bash-prod, bin/codex-session-init.bash-prod, bin/codex-stop-hook.bash-prod]
depends: [T002, T017, T018, T019, T020, T021, T022]
acceptance: [AC4, AC5, AC10, AC14]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L225
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L225](2026-04-30-port-shell-to-go-plan.md#L225) — Section: High-Level Approach — Phase 2 release gate
- Spec: N/A

### Files

- `bin/generate.bash-prod` (Delete from git)
- `bin/cerberus-task-completed-hook.bash-prod` (Delete)
- `bin/cerberus-teammate-idle-hook.bash-prod` (Delete)
- `bin/claude-session-init.bash-prod` (Delete)
- `bin/codex-session-init.bash-prod` (Delete)
- `bin/codex-stop-hook.bash-prod` (Delete)

### Depends

- T017, T018, T019, T020, T021, T022

### Goal

Ship Phase 2 as a tagged release. Hard cutover: 6 lifecycle bash production scripts deleted; all canonical shim paths now exec real Go binaries (except Amp dispatcher which lands in Phase 3).

### Implementation Notes

- Shims unchanged.
- Phase 2 release gate (load-bearing checklist subset):
  1. Hook fixture replay byte-equal for all 5 hook binaries (behavioral checklist item #8).
  2. Telemetry extractor parity vs 26 fixtures (item #4).
  3. Full bash test corpus passes (`bin/tests/run-all.sh`).
  4. Manual smoke: Claude Stop + Codex Stop + Codex SessionStart + TaskCompleted on an Amp team run.
- After this task ships, the six newly-ported `dist/<name>` artifacts MUST NOT match the compat-wrapper sentinel (CI assertion from T010).
- Tag: `git tag -a v$N -m "Phase 2 — Lifecycle hooks + generator ported to Go"`.

### Verification

- CI matrix green.
- `bash bin/tests/run-all.sh` green including all hook integration tests.
- Manual smoke transcripts captured in release notes.
- Pattern-rule precedence assertion green for the six new artifacts.
- Negative case: documented `git revert` rollback restores all 6 `.bash-prod` files.

---

## T024 — [integration-path-test] internal/debate skeleton + Integration via test-debate-byte-parity
```meta
phase: Phase 3 — Debate + Amp
files: [internal/debate/debate.go, internal/debate/types.go, internal/debate/coordinator.go, internal/debate/integration_test.go]
depends: [T011, T012, T015, T018]
acceptance: [AC7]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L568-L603
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L568-L603](2026-04-30-port-shell-to-go-plan.md#L568-L603) — Section: Technical Design — internal/debate
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L226](2026-04-30-port-shell-to-go-plan.md#L226) — Section: High-Level Approach — Phase 3 release gate
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L67](2026-04-30-port-shell-to-go-plan.md#L67) — Section: Testing Constraints — test-debate-byte-parity.sh as Phase 3 gate
- Spec: N/A

### Files

- `internal/debate/debate.go` (New) — exit code constants (5, 6, 130) + public API surface
- `internal/debate/types.go` (New) — `Coordinator`, `Request`, `RoundResult`, `Aggregate` types
- `internal/debate/coordinator.go` (New) — `Run(ctx, req) int` skeleton returning NotImplemented
- `internal/debate/integration_test.go` (New) — failing end-to-end test running the coordinator against a captured debate fixture and asserting via `test-debate-byte-parity.sh`-equivalent diff

### Depends

- T011 (state), T012 (reviewers), T015 (review-gate --debate currently bridges to bash), T018 (telemetry complete)

### Goal

Establish the **integration-path-test for Phase 3's debate feature**. Skeleton + integration test combined: package compiles, end-to-end test exercises composition root (`internal/debate.Run`) and FAILS for the right reason — proving the wiring path before T025-T027 fill in behavior.

### Implementation Notes

- Exit codes: `ExitAggregatorFailure = 5`, `ExitDegraded = 6` (preflight or mid-debate), `ExitCanceled = 130`. These MUST round-trip identically to bash.
- This task is the integration-path-test; T025-T027 fill in behavior; T028 wires `cmd/review-gate --debate` to call `internal/debate.Run` directly (replacing the Phase 1 bash bridge).
- The 3174-LOC debate library has well-defined states (preflight, round loop, abstain, degraded, cancel) — those map to the methods in `Coordinator` (`Preflight`, `RunRound`, `Aggregate`, `MarkDegraded`, `Cancel`).

### Verification

- `go vet ./internal/debate/...` clean.
- Integration test FAILS with NotImplemented (proves the wiring path).
- Public API surface matches the plan's signatures verbatim.

---

## T025 — internal/debate preflight + round loop + anonymized peer broadcasts
```meta
phase: Phase 3 — Debate + Amp
files: [internal/debate/preflight.go, internal/debate/round.go, internal/debate/anonymize.go, internal/debate/preflight_test.go, internal/debate/round_test.go, internal/debate/anonymize_test.go]
depends: [T024]
acceptance: [AC7]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L568-L603
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L568-L603](2026-04-30-port-shell-to-go-plan.md#L568-L603) — Section: Technical Design — internal/debate
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L982-L985](2026-04-30-port-shell-to-go-plan.md#L982-L985) — Section: Risks — debate fewer-than-two, cancellation, aggregator failure
- Spec: N/A

### Files

- `internal/debate/preflight.go` (New) — `Preflight(ctx, req)` (degraded exit 6 if fewer than two reviewers available)
- `internal/debate/round.go` (New) — `RunRound(ctx, req, round)`; orchestrates per-reviewer detached invocation via `reviewers.Runner`
- `internal/debate/anonymize.go` (New) — anonymized peer broadcasts (peer drafts presented without reviewer identity)
- `*_test.go` (New) — table-driven tests against debate sub-fixtures

### Depends

- T024

### Goal

Port the preflight phase, round loop, and anonymized peer broadcast logic from `bin/review-gate-debate.sh`.

### Implementation Notes

- TDD: write failing tests for preflight degraded exit (fewer-than-two reviewers → exit 6 with documented state transition); implement until green.
- Round loop calls `reviewers.Runner.Spawn` (Detach pattern from T006); per-round outputs go to `iteration-N/`.
- Anonymized peer broadcast must produce byte-equal anonymization output vs bash (gated by `test-debate-anonymization.sh`).

### Verification

- `go test ./internal/debate/... -race -cover` green for preflight and round loop tests.
- `test-debate-preflight.sh` passes.
- `test-debate-anonymization.sh` passes.

---

## T026 — internal/debate aggregation + final-round exclusion + exit code semantics
```meta
phase: Phase 3 — Debate + Amp
files: [internal/debate/aggregate.go, internal/debate/exit.go, internal/debate/aggregate_test.go]
depends: [T024]
acceptance: [AC7]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L568-L603
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L568-L603](2026-04-30-port-shell-to-go-plan.md#L568-L603) — Section: Technical Design — internal/debate
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L304-L321](2026-04-30-port-shell-to-go-plan.md#L304-L321) — README debate exit codes (5, 6 preflight, 6 mid-debate, 130) referenced
- Spec: N/A

### Files

- `internal/debate/aggregate.go` (New) — `Aggregate(ctx, req, final)`; preserves final-round exclusion behavior
- `internal/debate/exit.go` (New) — exit code dispatch (5 aggregator failure, 6 mid-debate degraded)
- `internal/debate/aggregate_test.go` (New)

### Depends

- T024

### Goal

Port the aggregation phase and the exact exit-code semantics. Aggregator failure (malformed repaired JSON) must preserve exit `5`, empty `reviews/`, and pending state behavior.

### Implementation Notes

- Use `jsonutil.Repair` (T005) for aggregator JSON repair.
- Final-round exclusion behavior must match bash (specific reviewers excluded from the final aggregation round under documented conditions).
- TDD: failing test that feeds malformed aggregator JSON and asserts exit 5 + empty reviews/ + pending state; implement.

### Verification

- `test-debate-aggregation.sh` passes.
- Negative test: aggregator fed malformed JSON → exit 5, empty `reviews/`, gate stays `pending`.
- Negative test: aggregator fed valid JSON with no consensus → documented degraded path, exit 6 mid-debate.

---

## T027 — internal/debate cancellation (SIGINT → exit 130) + degraded paths
```meta
phase: Phase 3 — Debate + Amp
files: [internal/debate/cancel.go, internal/debate/degraded.go, internal/debate/cancel_test.go]
depends: [T024]
acceptance: [AC7]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L568-L603
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L568-L603](2026-04-30-port-shell-to-go-plan.md#L568-L603) — Section: Technical Design — internal/debate
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L984](2026-04-30-port-shell-to-go-plan.md#L984) — Section: Risks — debate cancellation
- Spec: N/A

### Files

- `internal/debate/cancel.go` (New) — `Cancel(ctx, reviewDir)`; preserves SIGINT → exit 130, partial telemetry, pending state semantics
- `internal/debate/degraded.go` (New) — `MarkDegraded(ctx, reviewDir)`
- `internal/debate/cancel_test.go` (New) — tests for SIGINT-driven cancellation and degraded marking

### Depends

- T024

### Goal

Port cancellation and degraded paths. SIGINT must preserve exit `130`, partial telemetry behavior, and pending state semantics.

### Implementation Notes

- `context.Context` cancellation propagates through round loop and reviewer subprocess kills.
- Cancellation writes partial per-reviewer telemetry that survived; test asserts the partial telemetry is byte-equal vs bash for the same partial-completion state.
- TDD: failing test that simulates SIGINT mid-round and asserts (exit 130, partial telemetry written, gate state == "pending"); implement.

### Verification

- `test-debate-end-to-end.sh` cancellation cases pass.
- Negative test: SIGINT during round 1 → exit 130, partial telemetry, gate pending.
- Negative test: SIGINT during aggregation → exit 130, prior round telemetry preserved, gate pending.

---

## T028 — Wire review-gate --debate to internal/debate.Run + delete bin/review-gate-debate-bridge.sh
```meta
phase: Phase 3 — Debate + Amp
files: [cmd/review-gate/debate_bridge.go, bin/review-gate-debate-bridge.sh]
depends: [T002, T015, T025, T026, T027]
acceptance: [AC7]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L226
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L226](2026-04-30-port-shell-to-go-plan.md#L226) — Section: High-Level Approach — Phase 3
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L296-L297](2026-04-30-port-shell-to-go-plan.md#L296-L297) — Section: Architecture — bridge wrapper deleted in Phase 3
- Spec: N/A

### Files

- `cmd/review-gate/debate_bridge.go` (Modify, REPLACE bash bridge with `internal/debate.Run` direct call) — Go owns the entire `--debate` path now
- `bin/review-gate-debate-bridge.sh` (Delete) — no longer needed since `internal/debate.Run(ctx, ...)` is called directly

### Depends

- T015 (Phase 1 bash bridge), T025, T026, T027 (full debate impl)

### Goal

Replace the Phase 1+2 bash bridge with a direct call to `internal/debate.Run`. Remove the bridge wrapper. After this task, no Go code execs bash for debate — Phase 3 establishes the all-Go debate path.

### Implementation Notes

- Subcommand handler for `--debate` now constructs a `debate.Coordinator` and calls `Run(ctx, req)` directly; no `os/exec` of bash.
- Lock-deadlock rule still applies: Go's lock around `gate-state.json` must not be held during long-running debate phases (use `WithFileLock` only for the read-modify-write windows).
- Procgroup Detach pattern is used by `internal/reviewers.Runner.Spawn` for the per-reviewer subprocess; Supervise pattern is no longer used for debate after this task.
- The bash debate library `bin/review-gate-debate.sh` is still on disk (sourced by nothing now) — it's deleted in Phase 4 (T033) along with the other shared libs.

### Verification

- `dist/review-gate spawn-code-review --debate` runs with no `bash` subprocess in `pgrep` output.
- All debate parity tests still green: `test-debate-byte-parity.sh`, `test-debate-end-to-end.sh`, `test-debate-anonymization.sh`, `test-debate-aggregation.sh`, `test-debate-per-reviewer-golden.sh`, `test-debate-strategy.sh`, `test-debate-preflight.sh`.
- `bin/review-gate-debate-bridge.sh` no longer exists in git.
- Negative test: any callsite that still tries to invoke the bridge wrapper fails loudly (the file is gone, exec fails with ENOENT).

---

## T029 — [integration-path-test] cmd/cerberus-amp-toolbox + internal/ampbridge skeleton + integration via test-amp-shell-helper
```meta
phase: Phase 3 — Debate + Amp
files: [cmd/cerberus-amp-toolbox/main.go, internal/ampbridge/ampbridge.go, internal/ampbridge/types.go, internal/ampbridge/integration_test.go]
depends: [T011, T028]
acceptance: [AC11]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L656-L665
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L656-L665](2026-04-30-port-shell-to-go-plan.md#L656-L665) — Section: Technical Design — internal/ampbridge
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L890-L891](2026-04-30-port-shell-to-go-plan.md#L890-L891) — Section: API/Interface Design — Amp adapter
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L41](2026-04-30-port-shell-to-go-plan.md#L41) — Section: Assumptions — .sh extension on binary
- Spec: N/A

### Files

- `cmd/cerberus-amp-toolbox/main.go` (New) — thin entrypoint reading `TOOLBOX_ACTION` from env + JSON stdin → `ampbridge.Dispatch` → JSON stdout. Header comment documents that the shim filename keeps `.sh` on a binary.
- `internal/ampbridge/ampbridge.go` (New) — `Dispatch(action, req)` returning `Response` (skeleton: NotImplemented for all 6 actions)
- `internal/ampbridge/types.go` (New) — `Request`, `Response` shapes from `.amp/toolbox/cerberus.sh.bash-prod` stdin/stdout contract
- `internal/ampbridge/integration_test.go` (New) — failing end-to-end test exercising `cmd → ampbridge → stdout JSON`

### Depends

- T011 (cmd entrypoint pattern + state), T028 (debate fully ported so review-code/plan/spec actions can flow end-to-end)

### Goal

Establish the **integration-path-test for Phase 3's Amp feature**. Skeleton + integration: Go binary exists at `dist/cerberus-amp-toolbox` (replacing the Phase 0 compat-wrapper); integration test FAILS with NotImplemented for the 6 actions until T030.

### Implementation Notes

- Six actions: `review-code`, `review-plan`, `review-spec`, `ask-panel`, `status`, `clear-gate`.
- `TOOLBOX_ACTION` env var + JSON stdin input + JSON stdout output (Amp Toolbox protocol from `docs/AMP.md`).
- Filename `.sh` on a binary: shim at `.amp/toolbox/cerberus.sh` execs `dist/cerberus-amp-toolbox`. Header comment in `cmd/cerberus-amp-toolbox/main.go` notes this surprise; `docs/AMP.md` updated separately in T032.

### Verification

- `make install-binaries` produces `dist/cerberus-amp-toolbox` (real Go binary, not compat-wrapper).
- Pattern-rule precedence assertion: `dist/cerberus-amp-toolbox` no longer matches the compat-wrapper sentinel.
- Integration test FAILS with NotImplemented for each of the 6 actions.

---

## T030 — internal/ampbridge full TOOLBOX_ACTION dispatch (6 commands)
```meta
phase: Phase 3 — Debate + Amp
files: [internal/ampbridge/review_code.go, internal/ampbridge/review_plan.go, internal/ampbridge/review_spec.go, internal/ampbridge/ask_panel.go, internal/ampbridge/status.go, internal/ampbridge/clear_gate.go, internal/ampbridge/dispatch.go, internal/ampbridge/dispatch_test.go]
depends: [T029]
acceptance: [AC11]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L656-L665
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L656-L665](2026-04-30-port-shell-to-go-plan.md#L656-L665) — Section: Technical Design — internal/ampbridge
- Spec: N/A

### Files

- `internal/ampbridge/review_code.go`, `review_plan.go`, `review_spec.go`, `ask_panel.go`, `status.go`, `clear_gate.go` (New) — per-action implementations bridging to `cmd/review-gate` subcommand internals (or the underlying Go packages directly)
- `internal/ampbridge/dispatch.go` (New) — action → handler routing
- `internal/ampbridge/dispatch_test.go` (New) — table-driven tests for the 6 actions

### Depends

- T029

### Goal

Implement the 6 Amp Toolbox actions so byte-equal JSON output matches `bin/.amp/toolbox/cerberus.sh.bash-prod`.

### Implementation Notes

- TDD: failing per-action test (assert byte-equal JSON stdout vs captured bash output for the same `TOOLBOX_ACTION` + stdin); implement.
- Reuse `internal/state` + `internal/reviewers` + `internal/debate` (T028) directly; do NOT shell out to `dist/review-gate` (introduces an unneeded process boundary).

### Verification

- `test-amp-shell-helper.sh` passes against `dist/cerberus-amp-toolbox`.
- Per-action golden tests pass.
- T029 integration test passes (no longer NotImplemented).

---

## T031 — Phase 3 release gate: delete .amp/toolbox/cerberus.sh.bash-prod + full 24-cell golden parity + manual Amp smoke
```meta
phase: Phase 3 — Debate + Amp
files: [.amp/toolbox/cerberus.sh.bash-prod]
depends: [T002, T028, T030]
acceptance: [AC2, AC4, AC5, AC7, AC11, AC14]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L226
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L226](2026-04-30-port-shell-to-go-plan.md#L226) — Section: High-Level Approach — Phase 3 release gate
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L65-L67](2026-04-30-port-shell-to-go-plan.md#L65-L67) — Section: Testing Constraints — full 24-cell parity + test-debate-byte-parity
- Spec: N/A

### Files

- `.amp/toolbox/cerberus.sh.bash-prod` (Delete from git) — no longer needed since `cmd/cerberus-amp-toolbox` has landed; the Phase 0–2 Amp compat-wrapper at `dist/cerberus-amp-toolbox` is replaced by the real Go binary

### Depends

- T028, T030

### Goal

Ship Phase 3 as a tagged release. Hard cutover: Amp dispatcher bash deleted; debate bridge wrapper already deleted in T028. After this task, every binary in `GO_CMDS` is a real Go binary; the entire `dist/` tree contains zero compat-wrappers.

### Implementation Notes

- Phase 3 release gate (load-bearing checklist subset):
  1. Full 24-cell golden parity matrix byte-equal (item #7 — covers all 12 debate cells now).
  2. `test-debate-byte-parity.sh` (1018 LOC) green — the toughest existing parity test.
  3. `test-debate-end-to-end.sh`, `test-debate-anonymization.sh`, `test-debate-aggregation.sh`, `test-debate-per-reviewer-golden.sh`, `test-debate-strategy.sh`, `test-debate-preflight.sh` all green.
  4. `test-amp-shell-helper.sh` green.
  5. Manual smoke: Amp Toolbox round-trip (`/cerberus:review-code`, `/cerberus:status`, `/cerberus:clear-gate`).
- After this task ships, the pattern-rule precedence assertion (T010) covers the entire `GO_CMDS` list — NO `dist/<name>` may match the compat-wrapper sentinel.
- Tag: `git tag -a v$N -m "Phase 3 — Debate + Amp ported to Go"`.

### Verification

- CI matrix green.
- All 24 golden cells byte-equal.
- All debate parity tests green.
- Manual Amp smoke transcripts captured in release notes.
- Pattern-rule precedence assertion green for entire `GO_CMDS`.

---

## T032 — Edit docs/AMP.md (.sh extension on a binary)
```meta
phase: Phase 3 — Debate + Amp
files: [docs/AMP.md]
depends: [T031]
acceptance: [AC11]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L41
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L41](2026-04-30-port-shell-to-go-plan.md#L41) — Section: Assumptions — `.sh` filename on a binary
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L1087](2026-04-30-port-shell-to-go-plan.md#L1087) — Section: Deviation Log — Amp filename
- Spec: N/A

### Files

- `docs/AMP.md` (Modify) — document the `.sh` filename on a Go binary; explain that the Amp Toolbox protocol cares about executability + JSON stdio, not the filename extension; reference the Template B shim and Decision Log row

### Depends

- T031 (Phase 3 release gate)

### Goal

Document the `.sh`-on-binary surprise in `docs/AMP.md` so future maintainers understand why `.amp/toolbox/cerberus.sh` is a bash shim that execs a Go binary.

### Implementation Notes

- 2-3 paragraph addition near the existing "Toolbox protocol" section.
- Reference the Template B shim (Prerequisites) and the Decision Log row in the plan.
- No code changes.

### Verification

- `docs/AMP.md` includes the `.sh` filename note.
- `bin/tests/test-amp-shell-helper.sh` continues to pass.

---

## T033 — Phase 4 cleanup: delete 5 bash shared libs + verify no runtime path sources them
```meta
phase: Phase 4 — Cleanup
files: [bin/review-gate-lib.sh, bin/review-gate-models.sh, bin/review-gate-hook.sh, bin/review-gate-debate.sh, bin/telemetry-lib.sh]
depends: [T028, T031]
acceptance: [AC13]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L227
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L227](2026-04-30-port-shell-to-go-plan.md#L227) — Section: High-Level Approach — Phase 4 Cleanup
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L1062](2026-04-30-port-shell-to-go-plan.md#L1062) — Section: AC Coverage — AC #13 grep check
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L986](2026-04-30-port-shell-to-go-plan.md#L986) — Section: Risks — Cleanup phase races
- Spec: N/A

### Files

- `bin/review-gate-lib.sh` (Delete) — logic in `internal/cerberusenv` + `internal/state`
- `bin/review-gate-models.sh` (Delete) — logic in `internal/reviewers`
- `bin/review-gate-hook.sh` (Delete) — logic collapsed into `cmd/review-gate check`
- `bin/review-gate-debate.sh` (Delete) — logic in `internal/debate` (T024-T028)
- `bin/telemetry-lib.sh` (Delete) — logic in `internal/telemetry` (T018)

### Depends

- T031 (Phase 3 must be live for a release cycle; debate coordinator no longer invokes bash)

### Goal

Delete the now-orphan bash shared libraries. Verify zero runtime paths still source them so no in-flight subprocess can break.

### Implementation Notes

- Phase 4 ships ONLY after Phase 3 has been live for a full release cycle — the bash debate coordinator is no longer invoked, so cleanup-phase race risk (Risks > Cleanup phase races with in-flight reviewers) is mitigated by timing.
- AC #13 grep check (must return zero matches): `! grep -RIn 'review-gate-lib\.sh\|review-gate-models\.sh\|review-gate-debate\.sh\|review-gate-hook\.sh\|telemetry-lib\.sh' bin/ .amp/`
- This is the formal "no runtime path sources deleted bash libraries" verification.

### Verification

- `git ls-files bin/*.sh` shows no shared-lib `.sh` files.
- `! grep -RIn 'review-gate-lib\.sh\|review-gate-models\.sh\|review-gate-debate\.sh\|review-gate-hook\.sh\|telemetry-lib\.sh' bin/ .amp/` returns zero matches and exits 0.
- All external-surface `bin/tests/test-*.sh` continue to pass (those that did NOT exercise internal helpers).
- CI matrix green.

---

## T034 — Phase 4 cleanup: retire bash internal-helper test files (3 files)
```meta
phase: Phase 4 — Cleanup
files: [bin/tests/test-bash3-agent-parsing.sh, bin/tests/test-content-extraction.sh, bin/tests/test-iso8601-to-epoch.sh]
depends: [T012, T018, T031]
acceptance: [AC4]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L72
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L72](2026-04-30-port-shell-to-go-plan.md#L72) — Section: Testing Constraints — internal-helper bash tests retire when target deleted
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L1086](2026-04-30-port-shell-to-go-plan.md#L1086) — Section: Deviation Log — internal-helper bash tests replaced by Go
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L227](2026-04-30-port-shell-to-go-plan.md#L227) — Section: High-Level Approach — Phase 4 retire
- Spec: N/A

### Files

- `bin/tests/test-bash3-agent-parsing.sh` (Delete) — Go equivalent in `internal/reviewers/...` tests
- `bin/tests/test-content-extraction.sh` (Delete) — Go equivalent in `internal/telemetry/...` tests
- `bin/tests/test-iso8601-to-epoch.sh` (Delete) — Go equivalent in `internal/jsonutil/...` or `internal/telemetry/...` tests (per T018)

### Depends

- T031 (Phase 3 ships)

### Goal

Delete the bash internal-helper test files whose targets no longer exist (the helpers became Go packages with their own table-driven tests).

### Implementation Notes

- Verify the Go equivalents are checked in and green BEFORE deleting the bash versions:
  - bash3-agent-parsing → covered by `internal/reviewers/argv_golden_test.go` + helper tests
  - content-extraction → covered by `internal/telemetry/extract_test.go`
  - iso8601-to-epoch → covered by `internal/jsonutil/...` or `internal/telemetry/...` (per T018's iso8601 work)

### Verification

- 3 `.sh` files deleted; their Go equivalents exist and are green.
- `bash bin/tests/run-all.sh` continues to pass.

---

## T035 — Phase 4 cleanup: refresh README.md prereq table + final release tag
```meta
phase: Phase 4 — Cleanup
files: [README.md]
depends: [T033]
acceptance: [AC4, AC5, AC12]
plan_link: docs/2026-04-30-port-shell-to-go-plan.md#L194
```

### Source Documents

- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L194](2026-04-30-port-shell-to-go-plan.md#L194) — Section: Prerequisites — README install flow
- Plan: [docs/2026-04-30-port-shell-to-go-plan.md#L227-L228](2026-04-30-port-shell-to-go-plan.md#L227-L228) — Section: High-Level Approach — Phase 4 README refresh
- Spec: N/A

### Files

- `README.md` (Modify) — drop `jq`, `python3`, `setsid`, `gtimeout`, `flock(1)` from prereq table; add Go 1.25; document `make install-binaries` post-install step

### Depends

- T033 (deletions land first so the README accurately reflects the runtime state)

### Goal

Update `README.md` to reflect the new prereq surface (Go 1.25 in, jq/python3/setsid/gtimeout/flock(1) out). Tag the final release.

### Implementation Notes

- Edit only the prereq table and the install section; do NOT change the CLI surface, exit codes, env contract, or schema documentation (those remain stable per the No-Breaking-Changes constraint).
- Document `make install-binaries` as a one-step post-install action (per OQ-A resolution).
- Final tag after the README change lands and CI is green: `git tag -a v$N -m "Phase 4 — Cleanup; bash-to-Go port complete"`.

### Verification

- `README.md` install section names Go 1.25 as the only build prereq.
- README prereq table no longer mentions `jq`, `python3`, `setsid`, `gtimeout`, `flock(1)`.
- A user following the README install steps on a fresh machine without `jq` etc. successfully runs `dist/review-gate --help` (manual smoke).
- Final release tag pushed.

---

## Dependencies Graph

```text
T001 (module init)
 ├─ T002 (shims + .bash-prod) ──┐
 │   └─ T003 (update-plugin)    │
 ├─ T004 (cerberusenv)          │
 ├─ T005 (jsonutil) ──┐         │
 ├─ T006 (procgroup)  │         │
 ├─ T007 (state skeleton) ←─────┤  (depends on T005)
 │   └─ T008 (telemetry skel)   │
 ├─ T009 (goldens) ←────────────┤  (depends on T002, T005)
 └─ T010 (CI) ←─ T004,T005,T006,T007,T008,T009

T011 (review-gate skeleton+integration) ← T002, T003, T004, T006, T007, T009, T010
 ├─ T012 (reviewers)
 ├─ T013 (prompts)
 ├─ T014 (review-gate non-debate impl) ← T012, T013
 │   └─ T016 (Phase 1 release gate) ← T012, T013, T014, T015
 └─ T015 (debate Supervise bridge) ← T012, T014, T006

T017 (hooks skeleton+integration) ← T011
 ├─ T018 (telemetry complete) ← T008, T011
 ├─ T019 (codex hooks) ← T017, T018
 ├─ T020 (task-completed hook) ← T017, T018
 ├─ T021 (teammate-idle hook) ← T017, T018
 ├─ T022 (generate) ← T011, T012, T013, T018
 └─ T023 (Phase 2 release gate) ← T017, T018, T019, T020, T021, T022

T024 (debate skeleton+integration) ← T011, T012, T015, T018
 ├─ T025 (preflight + round + anonymize)
 ├─ T026 (aggregate + exit)
 ├─ T027 (cancel + degraded)
 └─ T028 (wire to internal/debate.Run, delete bridge wrapper) ← T015, T025, T026, T027

T029 (amp skeleton+integration) ← T011, T028
 └─ T030 (ampbridge full)

T031 (Phase 3 release gate) ← T028, T030
 └─ T032 (docs/AMP.md edit) ← T031

T033 (delete bash libs) ← T031
 └─ T035 (README + final tag) ← T033

T034 (retire bash helper tests) ← T031
```

## Acceptance Criteria Coverage

| Acceptance Criterion | Primary Task | Verified |
|----------------------|--------------|----------|
| AC #1: Same install paths executable on all four hosts | T002 | [ ] |
| AC #2: gate-state.json + on-disk artifacts byte-identical (after Canonicalize) | T005 | [ ] |
| AC #3: CERBERUS_* env contract + legacy aliases preserved | T004 | [ ] |
| AC #4: All existing bash external-surface tests pass | T009 | [ ] |
| AC #5: Each phase independently shippable and revertable | T016 | [ ] |
| AC #6: Reviewer subprocess lifecycle preserved | T006 | [ ] |
| AC #7: Debate coordinator byte-parity + canonical exit codes (5, 6, 130) | T024 | [ ] |
| AC #8: Cross-runtime flock interop holds during Phases 1–2 | T007 | [ ] |
| AC #9: Cold-start latency ≤ bash + 10ms | T010 | [ ] (warning-only initially) |
| AC #10: Hooks stdin/stdout/exit semantics unchanged | T017 | [ ] |
| AC #11: Amp Toolbox actions work via TOOLBOX_ACTION + JSON stdio | T029 | [ ] |
| AC #12: Source distribution + Go 1.25 + update-plugin rebuilds without dirtying tree | T001 | [ ] |
| AC #13: Phase 4 cleanup leaves no runtime path sourcing deleted bash libs | T033 | [ ] |
| AC #14: Behavioral test checklist passes on every per-phase release gate (not coverage %) | T010 | [ ] |

## File Impact Summary

| File | Status | Tasks |
|------|--------|-------|
| `go.mod`, `go.sum` | New | T001 |
| `Makefile` | New | T001 |
| `.gitignore` | Modify | T001 |
| `.github/workflows/test.yml` | New (skeleton T001, full T010) | T001, T010 |
| `bin/<name>` (7 shims) | New (replace existing) | T002 |
| `.amp/toolbox/cerberus.sh` (shim) | New (replace existing) | T002 |
| `bin/<name>.bash-prod` (7 renames) | Rename | T002 |
| `.amp/toolbox/cerberus.sh.bash-prod` | Rename | T002 |
| `bin/review-gate-debate-bridge.sh` | New | T002 |
| `bin/update-plugin` | Modify | T003 |
| `internal/cerberusenv/*` | New | T004 |
| `internal/jsonutil/*` | New | T005 |
| `internal/procgroup/*` | New | T006 |
| `internal/state/*` (skeleton) | New | T007 |
| `internal/state/{active,iteration,transitions}.go` | New | T011 |
| `internal/telemetry/*` (skeleton) | New | T008 |
| `internal/telemetry/{extract_*,iteration,run}.go` | New | T018 |
| `bin/tests/capture-goldens.sh`, `run-all.sh`, `golden/normalize.sh` | New | T009 |
| `bin/tests/fixtures/golden/` | New (populated) | T009 |
| `cmd/review-gate/*` | New | T011, T014, T015 |
| `internal/reviewers/*` | New | T012 |
| `internal/prompts/*` | New | T013 |
| `internal/host/*` | New | T011 |
| `bin/review-gate.bash-prod` | Delete | T016 |
| `internal/hooks/*` | New | T017, T019, T020, T021 |
| `cmd/claude-session-init/*` | New | T017 |
| `cmd/codex-session-init/*`, `cmd/codex-stop-hook/*` | New | T019 |
| `cmd/cerberus-task-completed-hook/*` | New | T020 |
| `cmd/cerberus-teammate-idle-hook/*` | New | T021 |
| `cmd/generate/*`, `internal/generator/*` | New | T022 |
| 6 lifecycle `.bash-prod` files | Delete | T023 |
| `internal/debate/*` | New | T024, T025, T026, T027 |
| `cmd/review-gate/debate_bridge.go` | Modify | T028 (replaces T015's bash bridge) |
| `bin/review-gate-debate-bridge.sh` | Delete | T028 |
| `cmd/cerberus-amp-toolbox/*`, `internal/ampbridge/*` | New | T029, T030 |
| `.amp/toolbox/cerberus.sh.bash-prod` | Delete | T031 |
| `docs/AMP.md` | Modify | T032 |
| 5 bash shared libs (review-gate-lib.sh etc.) | Delete | T033 |
| 3 bash internal-helper tests | Delete | T034 |
| `README.md` | Modify | T035 |

## Validation Artifacts

### Requirements Snapshot

| Requirement Type | Source | Text (paraphrased from plan) |
|------------------|--------|------------------------------|
| Objective | Plan §Context & Goals | Replace ~33,400 LOC of bash with a single Go module producing N per-binary executables, preserving the existing CLI surface, hook contracts, on-disk schemas, and host-neutral env contract byte-for-byte. |
| Objective | Plan §Scope (In Scope) | Port the seven in-scope `bin/` binaries + the Amp toolbox dispatcher; port the five shared libs into Go internal packages; add CI; capture 24-cell golden matrix. |
| Constraint | Plan §Implementation Constraints | Single Go module rooted at the repo; one per-cmd build into `dist.new/<name>` (sibling of `dist/` at repo root) atomically swapped to `dist/<name>`; canonical executable paths are committed bash shims; module deps allowlist `gofrs/flock` + `testify` + `go-cmp`. |
| Constraint | Plan §Implementation Constraints | No CLI changes; no schema changes; hook timeout caps preserved (TaskCompleted 2100s, TeammateIdle 10s). |
| MUST | Plan §Testing Constraints | The ten-class behavioral checklist must pass before any phase ships (env precedence, state transitions, reviewer argv goldens, telemetry extractor parity, lock interop, JSON canonicalization, 24-cell golden parity, hook fixture replay, bash acceptance suite, procgroup detach + supervise). |
| MUST | Plan §Testing Constraints | Every existing `bin/tests/test-*.sh` exercising external surface MUST continue to pass byte-for-byte against the Go binary. |
| MUST | Plan §Edge Cases | Reviewer subprocesses MUST detach via `Setsid` on both Linux and Darwin and survive parent exit. |
| MUST | Plan §Phase 1 cutover | Go MUST stay the parent of the bridged bash debate coordinator (Supervise pattern), forward signals, and propagate the bash exit code. |
| AC #1–#14 | Plan §Acceptance Criteria Coverage | See AC table above; all 14 ACs map to primary tasks. |

### Consistency Audit

| Item | Spec/Legacy | Plan | Status | Notes |
|------|-------------|------|--------|-------|
| Spec exists? | N/A — plan explicitly notes "Spec: N/A — derived from user description" | Plan §Context & Goals line 5 | Match | No spec to drift from; legacy contract surface enumerated in plan |
| CLI subcommand surface | `bin/review-gate` 12 subcommands | Preserved verbatim per Plan §API/Interface Design L887-L896 | Match | All 12 subcommands replicated in T011/T014 |
| Exit codes | README L304-L321 (debate 5/6/130) + L458-L464 (review-gate 0-4) | Preserved verbatim | Match | T026, T027 explicitly preserve debate codes; T014 preserves review-gate codes |
| Env aliases | README L478-L491 | Preserved verbatim | Match | T004 ports `__cerberus_resolve_*` |
| Hook timeouts | hooks/hooks.json L30 (2100s), L41 (10s) | Preserved | Match | T020/T021 set context.WithTimeout under each budget |
| Filename `.sh` on Amp binary | docs/AMP.md (toolbox protocol) | Filename keeps `.sh` extension on a Go binary | Deviation (logged + approved) | T029 + T032 document; Decision Log row 9 |
| Sole-process model on `--debate` | bash production runs as single process | Phases 1–2 run with Go parent + bash child via Supervise | Deviation (logged + approved) | T015 + T028; Deviation Log row 2 |
| Reviewer detach via `setsid` binary | bash uses `setsid` external | Go uses `syscall.SysProcAttr{Setsid: true}` directly | Deviation (logged + approved) | T006; Deviation Log row 1 |

### Deviation Log

(Inherited verbatim from plan §Deviation Log; see plan L1078-L1088. Summary:)

| Source | Deviation | Rationale | Approved? |
|--------|-----------|-----------|-----------|
| `bin/review-gate-debate.sh` `setsid …` | Go uses `syscall.SysProcAttr{Setsid: true}` directly on Linux + Darwin (Detach pattern) | Improved portability; no `setsid` binary dep | Yes |
| Bash sole-process model on `--debate` | Phases 1–2 = Go parent + bash child (Supervise pattern: `cmd.Run()` + `Setpgid` + signal forwarder) | Preserves Go-side cleanup invariants and signal-forwarding seam | Yes |
| `bin/telemetry-lib.sh` `flock` | Go uses `gofrs/flock` | `flock(1)` becomes a non-prereq | Yes |
| `bin/*-lib.sh` `python3 -c '...'` JSON repair | Go reimplements in `internal/jsonutil.Repair` | One fewer host prereq | Yes |
| `jq` filters | Most ported to `encoding/json` + `internal/jsonutil` | One fewer host prereq | Yes |
| `gtimeout` / `timeout` | Replaced by `context.WithTimeout`; deadline → exit 124 mapping preserved | Stdlib idiom | Yes |
| 3 internal-helper bash tests | Replaced by Go unit tests; bash versions deleted in Phase 4 | Internal helpers no longer exist | Yes |
| `.amp/toolbox/cerberus.sh` filename | Filename keeps `.sh` extension on a binary | Toolbox protocol cares about executability + JSON stdio | Yes |

### Obligation Coverage

| Plan Clause | Task(s) | Verification |
|-------------|---------|--------------|
| MUST preserve env contract byte-for-byte (§Context & Goals) | T004 | Env precedence table tests + alias divergence warning test (sync.Once) |
| MUST keep external bash test corpus passing (§Testing Constraints) | T009, T016, T023, T031 | `bash bin/tests/run-all.sh` is part of every per-phase release gate |
| MUST pass 10-class behavioral checklist (§Testing Constraints) | T010 wires CI; per-phase release gates (T016/T023/T031) run subset | Each per-phase release gate runs the relevant checklist subset |
| MUST replicate reviewer detach via Setsid on Linux + Darwin (§Assumptions) | T006 | Detach integration test asserts subprocess survives parent exit |
| MUST take same byte-range advisory lock on same path as bash (§Assumptions) | T007 | Cross-runtime flock interop test (Go gofrs/flock vs bash flock(1)) |
| MUST NOT group-kill on parent exit for reviewer subprocesses (§Assumptions) | T006, T012 | Detach test asserts no kill signal sent to detached children |
| MUST preserve hook timeouts (TaskCompleted 2100s, TeammateIdle 10s) (§Implementation Constraints) | T020, T021 | context.WithTimeout under each budget; tests assert deadline behavior |
| MUST NOT change CLI surface (§Implementation Constraints) | T011, T014 | Subcommand list reproduced verbatim; argv goldens; help text byte-identical |
| MUST NOT change schema (§Implementation Constraints) | T005, T007 | MarshalStable + Gate struct field tags + 24-cell golden diff |
| MUST preserve debate exit codes 5, 6 (preflight + mid-debate), 130 (§Edge Cases) | T026, T027 | Per-code negative tests + test-debate-byte-parity |
| MUST take pre-pull Go-toolchain guard in update-plugin (§Prerequisites) | T003 | Manual test removing `go` from PATH; test-update-plugin.sh case |
| MUST detect shim/dist drift via sentinel comment (§Risks) | T002, T003 | Sentinel grep in update-plugin; manual test replacing a shim |
| MUST NOT hold gate-state.json lock across bash bridge exec (§Risks) | T015 | Lock-deadlock end-to-end test instrumented to log enter/exit |
| MUST verify pattern-rule precedence (Go-build vs compat-wrapper) (§Risks) | T010 | CI assertion greps each `dist/<name>` for compat-wrapper sentinel; incremental per phase |
| MUST capture 24-cell golden matrix BEFORE replacing any production script (§Prerequisites) | T009 | T009 ordering: depends on T002 (rename), runs before T011 (Phase 1 begins) |
| MUST delete bash production binary in same phase as Go port lands (§High-Level Approach) | T016 (Phase 1), T023 (Phase 2), T031 (Phase 3) | Per-phase release gate task explicitly deletes the corresponding `.bash-prod` files |
| MUST retire internal-helper bash tests only when target deleted (§Testing Constraints) | T034 | Phase 4 timing; depends on T031 |

### System Wiring Coverage

| Flow | Wiring Task(s) | Verification |
|------|----------------|--------------|
| Marketplace install → committed shim → `dist/<name>` → bash compat-wrapper or Go binary | T001 (Makefile), T002 (shims), T010 (CI verifies `test -x` for both shim and dist artifact) | Pattern-rule precedence assertion; sentinel grep |
| `git pull` → pre-pull Go guard → pull → `make install-binaries` → atomic `dist.new/` → `dist/` swap → rebuild_rc check | T001 (Makefile atomic swap), T003 (update-plugin guards) | Manual test removing `go` from PATH; test-update-plugin.sh; rebuild failure UX message |
| Hook stdin → `cmd/<hook>` → `cerberusenv.Resolve` → `internal/hooks.Handle*` → `internal/state.Store` → stdout JSON | T011 (entrypoint pattern), T017 (hooks skeleton + integration), T019/T020/T021 (per-hook impl) | Hook fixture replay against `bin/tests/fixtures/`; byte-equal stdout/stderr/exit |
| `review-gate --debate` → Phases 1–2: Go parent (Supervise) + bash bridge wrapper child + bash debate library; Phase 3: direct internal/debate.Run | T015 (Phase 1+2 bridge), T028 (Phase 3 direct call + bridge wrapper deletion) | End-to-end smoke; signal forwarding test; exit code propagation test; lock-deadlock test |
| Reviewer subprocess: cmd → reviewers.Runner.Spawn → procgroup.Detach (Setsid both platforms) → claude/codex/gemini child detached, survives parent | T006 (procgroup), T012 (reviewers.Runner) | Detach integration test (parent exits, child survives, recorded sid != parent sid) |
| Cross-runtime locking: Go gofrs/flock + bash flock(1) on same `gate-state.json` path | T007 (lock interop test), T015 (Go releases lock before bridge exec) | Linux + macOS interop test; lock-deadlock test |
| Golden capture (Phase 0) → 24 cells of bash output → normalize via `Canonicalize` → byte diff against Go output in every later phase | T005 (Canonicalize), T009 (capture + run-all + normalize.sh), T010 (CI golden-diff) | `make golden-diff` is part of every per-phase release gate |

### Propagation Map

| Input/Field/Signal | Origin | Transport | Consumption | Verification |
|--------------------|--------|-----------|-------------|--------------|
| `CERBERUS_RUN_KEY` (and alias `REVIEW_GATE_SESSION_KEY`) | env (parent process) | `cerberusenv.Resolve` → `Env.RunKey` → `state.Store` (review dir resolution) | Reviewer subprocess env via `ExportNeutralEnv` | T004 env precedence table tests; T015 env audit test |
| `CERBERUS_ROOT` (and alias `CLAUDE_PLUGIN_ROOT`) | env | `cerberusenv.Resolve` → `Env.Root` | Bridge exec (`bash $CERBERUS_ROOT/bin/review-gate-debate-bridge.sh`); shim `target` resolution | T004; T015 bridge end-to-end test |
| `CERBERUS_SESSION_ID` (and alias `CLAUDE_SESSION_ID`) | env | `cerberusenv.Resolve` → `Env.SessionID` | review dir lookup; hook payload echo | T004 |
| `CERBERUS_TRANSCRIPT_PATH` (aliases `CLAUDE_TRANSCRIPT_PATH`, `REVIEW_GATE_TRANSCRIPT_PATH`) | env | `cerberusenv.Resolve` → `Env.TranscriptPath` | hook handler use | T004 |
| `gate-state.json` `status` transitions | bash/Go writers | `state.WithFileLock` + `Gate.MarshalJSON` (ordered) | active-gate lookup; `wait`/`status` subcommands | T011 state-machine tests + 24-cell golden diff |
| Reviewer argv per (agent, mode, type) | `internal/reviewers.BuildXCommand` | `*exec.Cmd.Args` | child process | T012 argv goldens |
| Debate exit codes (5, 6, 130) | bash debate library / `internal/debate` | exit code returned to parent | shell test assertions | T026/T027 + test-debate-byte-parity |
| Reviewer subprocess detach | `cmd → procgroup.Detach → SysProcAttr.Setsid` | `os/exec.Cmd.Start()` then `Process.Release()` | reviewer child running in own session post parent exit | T006 detach integration test |
| Signal forwarding (Phase 1+2 bridge) | parent SIGINT/SIGTERM/etc | `signal.Notify` channel → `syscall.Kill(-cmd.Process.Pid, sig)` → bash child pgid | bash trap handlers in coordinator | T015 signal forwarding test |
| Golden canonical mask | `internal/jsonutil/canonical.go` field-mask list | `Canonicalize` applied to both bash-captured golden and Go-produced output | byte diff in `make golden-diff` | T005 canonical tests + T009 normalize.sh |

### Sizing Summary

| Task | Files | Subsystems | ACs | Mechanical? | Status | Action |
|------|-------|------------|-----|-------------|--------|--------|
| T001 | 5 | 1 (build) | 1 | No | OK | — |
| T002 | 9 | 2 (bin, .amp) | 2 | Yes (template apply) | OK | Mechanical sweep allowed |
| T003 | 1 | 1 (bin) | 1 | No | OK | — |
| T004 | 5 | 1 (cerberusenv) | 1 | No | OK | — |
| T005 | 7 | 1 (jsonutil) | 1 | No | OK | — |
| T006 | 5 | 1 (procgroup) | 1 | No | OK | — |
| T007 | 6 | 1 (state) | 2 | No | OK | — |
| T008 | 3 | 1 (telemetry) | 1 | No | OK | — |
| T009 | 4 | 1 (bin/tests) | 2 | No | OK | — |
| T010 | 3 | 2 (CI, bench) | 2 | No | OK | — |
| T011 | 9 | 3 (cmd, state, host) | 2 | No | OK | At hard file limit; balanced because skeleton+integration is one outcome |
| T012 | 9 | 1 (reviewers) | 1 | No | OK | — |
| T013 | 3 | 1 (prompts) | 1 | No | OK | — |
| T014 | 9 | 1 (cmd/review-gate) | 3 | No | OK | — |
| T015 | 3 | 2 (cmd/review-gate, cerberusenv) | 3 | No | OK | — |
| T016 | 1 | 1 (bin) | 3 | No | OK | — |
| T017 | 5 | 2 (hooks, cmd) | 1 | No | OK | — |
| T018 | 7 | 1 (telemetry) | 2 | No | OK | — |
| T019 | 5 | 2 (cmd, hooks) | 1 | No | OK | — |
| T020 | 3 | 2 (cmd, hooks) | 1 | No | OK | — |
| T021 | 3 | 2 (cmd, hooks) | 1 | No | OK | — |
| T022 | 7 | 2 (cmd/generate, generator) | 1 | No | OK | — |
| T023 | 6 | 1 (bin) | 4 | Yes (delete sweep) | OK | Mechanical sweep + release-gate exception (4 ACs reflect Phase 2 release-gate concerns: AC4 golden parity, AC5 rollback, AC10 hook fixture replay, AC14 behavioral checklist; deletions ship atomically so splitting would couple the cutover artificially) |
| T024 | 4 | 1 (debate) | 1 | No | OK | — |
| T025 | 6 | 1 (debate) | 1 | No | OK | — |
| T026 | 3 | 1 (debate) | 1 | No | OK | — |
| T027 | 3 | 1 (debate) | 1 | No | OK | — |
| T028 | 2 | 2 (cmd/review-gate, bin) | 1 | No | OK | — |
| T029 | 4 | 2 (cmd, ampbridge) | 1 | No | OK | — |
| T030 | 8 | 1 (ampbridge) | 1 | No | OK | — |
| T031 | 1 | 1 (.amp) | 6 | No | OK | Multiple ACs OK because all are Phase 3 release-gate verifications |
| T032 | 1 | 1 (docs) | 1 | No | OK | — |
| T033 | 5 | 1 (bin) | 1 | Yes (delete sweep) | OK | Mechanical sweep |
| T034 | 3 | 1 (bin/tests) | 1 | Yes (delete sweep) | OK | Mechanical sweep |
| T035 | 1 | 1 (root docs) | 3 | No | OK | — |

All tasks within hard limits (12 files standard / 18 files mechanical / 3 subsystems / 3 ACs except release-gate verification tasks which legitimately span the phase's ACs).

## Notes

- Total tasks: 35
- Execution model: `/cerberus:run-team` may run independent same-phase tasks in parallel when dependencies and file scopes allow (e.g., T004/T005/T006 in Foundation; T019/T020/T021 in Phase 2 hooks)
- Each implementer commit must include a `Cerberus-Task: T###` trailer
- Each phase ends with a tagged release; rollback = `git revert` of the phase commit + version pin to the prior tag (no `<name>.bash` runtime fallback)
- Coverage percentage is supporting CI evidence only — the 10-class behavioral checklist is the load-bearing release gate
