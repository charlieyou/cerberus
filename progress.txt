## Codebase Patterns
- Plugin entry points live in `.amp/plugins/` and require the `// @i-know-the-amp-plugin-api-is-wip-and-very-experimental-right-now` header
- Use `import.meta.dir` to resolve plugin root; prompts live in `prompts/` relative to project root
- State directory: `.tmp/cerberus/<thread-id>/` for per-thread gate persistence
- Typecheck verification: `PLUGINS=all amp plugins list` confirms plugin loads; `PLUGINS=all amp plugins exec <plugin> <event> --data '{}'` tests execution
- Sanitize thread IDs with `id.replace(/[^a-zA-Z0-9._-]/g, '_')` for filesystem paths
- Export shared utilities (getPluginRoot, loadPrompt, resolveThreadStateDir) from cerberus.ts for use by future library modules
- Gate library lives at `.amp/plugins/lib/gate.ts` — exports createGateState, save/load/active gate helpers, and GateLock/withGateLock for locking
- Gate state is scoped per thread+gateId: `.tmp/cerberus/<thread>/<gateId>/gate-state.json`
- Active gate pointer: `.tmp/cerberus/<thread>/active-gate` — plain text file with the gate ID
- Run tests with `bun test ./.amp/plugins/lib/gate.test.ts` (note leading `./` required)
- Model resolution: `.amp/plugins/lib/models.ts` — resolveModels(mode, env) returns per-reviewer model/effort settings
- Review JSON parsing: `.amp/plugins/lib/review-json.ts` — parseReviewOutput(text) is the all-in-one entry point
- Reviewer runners: `.amp/plugins/lib/runners.ts` — runReviewer(opts) for single, runAllReviewers(opts) for parallel execution
- Runner ExecFn type is `(cmd, opts) => Promise<{stdout,stderr,exitCode}>` — inject ctx.$ or mock in tests
- Runner results written to `<thread>/<gateId>/reviews/<reviewer>.json` + `.done`/`.failed` sentinels
- Commands: `amp.registerCommand(id, { title, category, description }, handler)` — handler receives `ctx` with `.thread`, `.ui`, `.$`
- `ctx.ui.input()` returns `undefined` when cancelled — always guard with `if (x === undefined) return`

---

## 2025-04-14 - US-003
Thread: https://ampcode.com/threads/T-019d8cea-ac82-716a-bb16-77ca42893cca
- Implemented production Cerberus plugin entry point at `.amp/plugins/cerberus.ts`
- Registered `session.start` handler, exported shared path resolution helpers (getPluginRoot, getPromptsDir, getStateDir, loadPrompt, resolveThreadStateDir)
- Verified prompt loading works (tested with `generators/healthcheck.md`)
- Plugin loads successfully with `PLUGINS=all amp plugins list`
- Files changed: `.amp/plugins/cerberus.ts` (new)
- **Learnings for future iterations:**
  - `@ampcode/plugin` types are resolved by Amp's Bun runtime — no `package.json` or `tsconfig.json` needed in this repo
  - `import.meta.dir` points to `.amp/plugins/`, so resolve `../..` to get project root
  - `PLUGINS=all amp plugins list` is the quickest way to verify a plugin is valid and active
  - The spike plugin at `cerberus-spike.ts` is still present for reference — don't break it
---

## 2025-04-14 - US-004
Thread: https://ampcode.com/threads/T-019d8cf4-598f-7208-8ebe-8c6d926722db
- Implemented review gate storage and locking library at `.amp/plugins/lib/gate.ts`
- GateState type models: version, status, triggerSource, reviewers, consensus, decision, iteration, timestamps, lastTurnId
- Active gate tracking via plain-text pointer file per thread
- GateLock class with stale-lock detection (30s) and withGateLock convenience wrapper
- 18 tests covering read/write, active gate, and lock behavior in `.amp/plugins/lib/gate.test.ts`
- Files changed: `.amp/plugins/lib/gate.ts` (new), `.amp/plugins/lib/gate.test.ts` (new)
- **Learnings for future iterations:**
  - Bun test requires `./` prefix for dotfile paths (e.g., `bun test ./.amp/...`)
  - Gate state is scoped under `<thread>/<gateId>/` subdirs to support multiple gates per thread
  - Lock uses write-then-verify pattern for basic mutual exclusion (not atomic on all filesystems but sufficient for single-machine plugin use)
  - The bash implementation stores gate-state.json at session level; the TS version nests under gateId for cleaner separation
---

## 2025-04-14 - US-005
Thread: https://ampcode.com/threads/T-019d8cf7-407a-7480-ba2f-dee023b8c114
- Implemented model resolution in `.amp/plugins/lib/models.ts` — normalizeMode, resolveModels for fast/smart/max with env var overrides
- Implemented review JSON parsing in `.amp/plugins/lib/review-json.ts` — extractLastJsonObject, unwrapReviewJson, validateReviewResult, parseReviewOutput
- 11 model tests + 30 review-json tests all passing
- Files changed: `.amp/plugins/lib/models.ts` (new), `.amp/plugins/lib/models.test.ts` (new), `.amp/plugins/lib/review-json.ts` (new), `.amp/plugins/lib/review-json.test.ts` (new)
- **Learnings for future iterations:**
  - `extractLastJsonObject` scans for all `{` chars and tries JSON.parse at each — prefers review candidates (objects with `verdict`, `structured_output`, or `response` keys) over generic objects
  - Model resolution uses two-tier env vars: `*_OVERRIDE` takes precedence over base `*` var, which takes precedence over mode defaults
  - The `findJsonEnd` helper does brace-depth tracking with string/escape awareness for reliable JSON boundary detection
  - `unwrapReviewJson` handles three wrapper types: `structured_output` (codex), `response` (gemini), `result` (claude) — in that order
  ---

  ## 2025-04-14 - US-006
  Thread: https://ampcode.com/threads/T-019d8cfa-7f8d-725e-964a-f86be58c05d0
  - Implemented reviewer process runners at `.amp/plugins/lib/runners.ts`
  - `buildReviewerCommand` builds CLI args+env for codex, gemini, claude matching bash `spawn_reviewer`
  - `runReviewer` executes via injected ExecFn, handles exit codes, parse failures, and timeouts
  - `runAllReviewers` runs reviewers in parallel via Promise.all with shared model resolution
  - `writeRunnerResult` / `readRunnerOutput` for gate-compatible file persistence
  - 21 tests covering command building, success/failure/timeout, parallel execution, and file I/O
  - Files changed: `.amp/plugins/lib/runners.ts` (new), `.amp/plugins/lib/runners.test.ts` (new)
  - **Learnings for future iterations:**
  - ExecFn abstraction makes runners fully testable without spawning real processes
  - Gemini needs GEMINI_CLI_SYSTEM_SETTINGS_PATH env var pointing to `config/gemini-readonly-settings.json`
  - Claude reviewer uses --allowedTools/--disallowedTools flags for read-only enforcement
  - Codex uses `-s read-only` plus `--output-schema` for structured output
  - Runner results follow the same `.json` + `.done`/`.failed` sentinel pattern as the bash implementation
  - Consensus library: `.amp/plugins/lib/consensus.ts` — evaluateConsensus(inputs, mode) for majority/all/any; formatReviewFeedback for markdown output
  - Blocking = P0/P1 findings; any FAIL verdict or P0/P1 finding forces requires_decision regardless of consensus mode
  - Majority mode: all-PASS or 2+ PASS with only P2/P3 findings → auto_approve; otherwise requires_decision
     ---

  ## 2025-04-14 - US-007
  Thread: https://ampcode.com/threads/T-019d8cfd-40a0-770d-be19-d493b928df65
  - Implemented consensus evaluation and review feedback formatting at `.amp/plugins/lib/consensus.ts`
  - `evaluateConsensus(inputs, mode)` supports majority, all, and any consensus modes
  - FAIL verdicts and P0/P1 findings always force `requires_decision` regardless of mode
  - Majority mode: all-PASS or 2+ PASS with only P2/P3 → auto_approve
  - `formatReviewFeedback` produces markdown with results table, blocking issues, informational items, and round counter
  - Helpers: `isBlocking`, `collectBlockingFindings`, `collectAllFindings`, `formatResultsTable`, `formatBlockingIssues`, `formatInformationalFindings`
  - 34 tests covering all consensus modes, P0/P1 blocking, feedback formatting, max-round, and mixed-verdict scenarios
  - Files changed: `.amp/plugins/lib/consensus.ts` (new), `.amp/plugins/lib/consensus.test.ts` (new)
  - **Learnings for future iterations:**
    - Consensus logic mirrors bash `calculate_consensus` in review-gate-hook.sh (lines 916-1072)
    - ReviewerInput type decouples consensus from RunnerResult — can be built from gate state or runner output
    - formatReviewFeedback handles three branches: auto_approve, max-rounds-reached, and requires_decision (with blocking/informational split)
    - The bash implementation also has `collect_issues`, `collect_blocking_issues`, `collect_informational_findings` — all ported as TypeScript equivalents
  ---

## 2025-04-14 - US-008
Thread: https://ampcode.com/threads/T-019d8d00-fef6-746e-97d4-12cd70560540
- Registered `review-code` command under the Cerberus category in `.amp/plugins/cerberus.ts`
- Command collects review options via 5 sequential `ctx.ui.input` prompts: diff mode, intelligence mode, reviewers, consensus mode, and max rounds
- Creates a new gate via `createGateState`, sets it as active gate, and spawns reviewers with `runAllReviewers`
- Runner results written via `writeRunnerResult` and gate state updated with output file references
- Added `parseReviewers` helper for comma-separated reviewer input validation
- Files changed: `.amp/plugins/cerberus.ts` (modified)
- **Learnings for future iterations:**
  - `amp.registerCommand` takes (id, metadata, handler) — metadata needs `title`, `category`, `description`
  - `ctx.ui.input` returns `undefined` when cancelled — always check before proceeding
  - `ctx.$` is the shell exec function available in command context; wrap it to match the `ExecFn` signature for runners
  - GateState doesn't currently store consensus mode or maxRounds — future stories (US-009) will need to extend GateState or store these in a separate config file
---
